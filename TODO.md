# TODO — MCNuX build plan

All foundations and Tier-1 transport-chain enablers are in: units/RNG/stats/tetrad headers, subcycling guard + gate, transport cadence group, pure-SoA `PacketContainer` + runtime per-patch owner, WeakLibInterp wrappers, coefficient dispatch (analytic + baseline table + range policy), TRP relabeling map, source-term ledger + zero-then-add, geodesic push, and both TestMCNuX initial-data writers (`schwarzschild.cxx`, `hydro_profiles.cxx`). Five harness tests are green (`unit-selftest` battery SIZE=106, `source-zero-add`, `minkowski-freestream`, `schwarzschild-id`, `hydro-sphere-id`). This plan run (2026-08-24, second of the day) re-verified every slice against live code: no drift against DONE records; the spec corpus (README + 12 coded specs) remains complete and coverage-closed — no spec needs authoring; the only spec defects remain the in-place text fixes in T28. The entire physics event chain is still unimplemented — every physics primitive exists as a tested library with zero production consumers (RNG `u()`, `evaluate_coefficients`, `RangedTableCoefficients`, tetrad transforms, `relabel()`, ledger deltas all have no callers outside headers and selftests). New this run: the fluid-state/four-velocity gather (T33) is a hard structural blocker on the whole interaction chain (MCNuX inherits only ADMBaseX; nothing reads HydroBaseX at runtime), and T14 is split into a pure core (T14a) and a scheduled half (T14b) mirroring the T18a split. Critical path: T18a-core ∥ T33 ∥ T14a → T14b → T18a-int → T18b/T18c; T16, T19 (skeleton), T20, T21b, T22a, T30, T31, T32 and the Tier-5 gates are unblocked in parallel.

## Standing facts every item inherits

- Acceptance is entirely programmatic: exact/bitwise (RNG KATs, enumerations, α=1 parity), machine ~1e-14, golden 1e-12 (Cactus harness ABSTOL=RELTOL=1e-12), relaxed 1e-10, 4σ statistical at `N_p=1048576` seed `1296518744` (secondary `20260730`), conservation 1e-13/step. No human-judgment criteria exist in this corpus; all `stats-*` work reuses the 4σ z-score pattern — never bitwise assertions on grid tallies ([MCNX-RNG-08]).
- Shared utilities are implemented ONCE and reused — never re-author: constants/units/species = `MCNuX/src/mcnux_units.hxx` (`degeneracy`, `lepton_number`, `rtol_machine`/`rtol_pinned`, `detail::approx_eq`, `h_cgs`/`hc_MeV_cm`/`pinned::hc_MeV_cm`, `length_code_to_cgs`); Philox `u(S,q,e,k)` = `MCNuX/src/mcnux_rng.hxx` (`:204-207`); tetrad/null closure = `MCNuX/src/mcnux_tetrad.hxx` (`spatial_metric_inverse`, `p_t_closure` recomputed per call, `build_tetrad` with Gram–Schmidt pinned x→y→z, `fluid_frame_direction(u1,u2)`, `fluid_momentum_to_coordinate`, `metric_dot` — uniforms in / momenta out, NO RNG calls or `(e,k)` map inside: emission k=4/5, scattering k=2/3, diffusion k=1/2 belong to callers; callers may cache the returned PODs); TRP relabeling = `MCNuX/src/mcnux_trp.hxx` (`relabel(eta,kappa_a,kappa_s,alpha)`, 2021 α convention, α = retained fraction); ledger arithmetic = `MCNuX/src/mcnux_srcterms.hxx` (`emission/absorption/scattering_delta`, `source_rate`, `source_from_delta`; g=4 never enters ledger arithmetic); WLI call boundary = `MCNuX/src/mcnux_opacity.hxx` (`eos_evaluate`, `eos_invert_*`, `emab_evaluate`/`iso_offset`/`iso_evaluate`, `nes_pair_evaluate`/`nes_detailed_balance_fill`/`pair_crossing_fill`, `brem_evaluate_summed`, `AxisBounds` endpoint readers; species slots νe=0/ν̄e=1, νx none); packet container = `MCNuX/src/mcnux_particles.hxx` (`PacketContainer : amrex::ParticleContainerPureSoA<7,2>`, positions in real slots 0..2, `PIdx` px..w = 3..6, `IntIdx` species/event_counter = 0..1, id via `idcpu`); 4σ z-score/estimator machinery = `MCNuX/src/mcnux_stats.hxx` (`ZScore`, `zscore(estimate,expected,sigma)`, `stats_sigma_bound`, pinned `stats_seed_primary`/`stats_seed_secondary`/`stats_default_num_packets`; σ formulas stay per-benchmark, never baked in); coefficient interface = `MCNuX/src/mcnux_coefficients.hxx` (`Coefficients`, `FluidState`, `AnalyticOpacityParams`, `CoefficientSource`, `evaluate_coefficients` dispatch at `:145-153`; parameter glue in `mcnux_coefficients.cxx`); baseline table-source assembly = `MCNuX/src/mcnux_table_coeffs.hxx` (`BaselineTableViews` caller-owned table-view bundle, `BaselineTableCoefficients` TableEval callable — [MCNX-OPA-04/05]); range policy = `MCNuX/src/mcnux_table_range.hxx` (`ClampCounters`, `rho_min_cgs`, `RangedTableCoefficients` ranged TableEval, `inversion_usable` — [MCNX-OPA-06/07]; table consumers use it, never bare `BaselineTableCoefficients`). Any new physical constant lands in `mcnux_units.hxx` exactly once. Analytic mode ([MCNX-VER-05]) suffices for the whole T14–T18 chain; table residency (T31) is off the critical path.
- Runtime-wiring facts: the runtime `PacketContainer` owner is `MCNuX::packet_population(int patch)` / `num_packet_patches()` (declared `mcnux_particles.hxx`, defined `mcnux_geodesic.cxx:64-90`, lazy per-patch `PacketContainer(ghext->patchdata.at(p).amrcore->GetParGDB())`, only after the grid exists) — never construct a second container. Geodesic push = `mcnux_geodesic.hxx` (`VertexMetricGather`, `geodesic_rhs`, RK4 `geodesic_step` templated on the gather; never re-author the gather or RHS); the tile-walk idiom for particle operators is `for_each_packet_tile` + `amrex::ParallelFor` in `mcnux_geodesic.cxx` — never `loop_all_device` for packets (no cell-iteration idiom for particle creation exists yet — T14b establishes it; append precedent is the pinned-tile pattern at `mcnux_geodesic.cxx:302-325`). The synthetic seeding fixture (`MCNuX_SeedSyntheticPackets`, gated `test_synthetic_packets`) and the `mcnux_packet_diag` grid array (`pk_x..pk_pz, pk_pt = −α²p^t + β^ip_i`, row = id−1) are the transport-test observable precedent. `exemplar_packet_kernel` (`mcnux_particles.hxx:112-124`) is compile-only scaffolding; T18a-int supersedes its event_counter increment — remove or wire it there. No fluid/HydroBaseX gather exists (`interface.ccl:13` inherits ADMBaseX only) — T33 supplies it; T14b, T18a-int, T18c consume it.
- Selftest battery protocol (battery split per-domain): `MCNuX/src/mcnux_selftest.cxx` owns the APPEND-ONLY index→name row table and the ordered `append_*_rows` call sequence in `run_battery()` (that sequence IS the row ordering); check code lives in per-domain `MCNuX/src/mcnux_selftest_<domain>.cxx` files with the shared `Battery`/`synth_tval` machinery and appender declarations in `mcnux_selftest.hxx`. To append rows: add a new `append_*_rows` function (in the matching existing domain file, or a new `mcnux_selftest_<domain>.cxx` added to `make.code.defn`), declare it in `mcnux_selftest.hxx`, call it at the END of `run_battery()`, extend the row table in `mcnux_selftest.cxx`, bump `SIZE=` in `MCNuX/interface.ccl` (`mcnux_selftest` group; currently 106; next-free row index 106), regenerate the golden TSV from a sandbox harness run — never hand-edit, never insert rows into an existing appender; routine hard-errors on table/SIZE mismatch; sequential tasks rebase on the then-current `SIZE`. Header convention (thirteen shared headers: twelve with compile-time selftests + `mcnux_selftest.hxx`): flat `MCNuX/src/mcnux_*.hxx`, `namespace MCNuX`, aggregated through one `#include` each in `stub.cxx` (AMReX precision-gate legs live in `mcnux_particles.hxx`; `stub.cxx` keeps the double/CCTK_REAL legs). Battery rows should prefer booleans/exact values so the golden stays bitwise-stable across libm ulp differences.
- Parfile facts every parfile inherits: `Cactus::presync_mode = "mixed-error"` (required to survive CarpetX's own paramcheck), `IO::out_fileinfo = "axis labels"`, `IO::out_dir = $parfile`, `IO::parfile_write = no`, `CarpetX::out_metadata = no`; every MCNuX parfile must activate `ADMBaseX` (MCNuX `INHERITS: ADMBaseX`), and any run that reads the metric (transport) also needs `ODESolvers` (ADMBaseX initial data is scheduled only `IN ODESolvers_Initial`; zero evolved variables is a warning, not an abort) — `ActiveThorns = "CarpetX IOUtil ODESolvers ADMBaseX MCNuX"` is the Minkowski transport baseline (`MCNuX/test/minkowski-freestream.par`); `CarpetX::restrict_during_sync = no` whenever subcycling is on; any parfile activating `TestMCNuX` must live under `MCNuX/test/*.par` ([MCNX-BLD-06]) and also activate `HydroBaseX` (`TestMCNuX INHERITS: ADMBaseX HydroBaseX`). Test-only behaviour is gated by `STEERABLE=NEVER` booleans following the `test_synthetic_deposit`/`test_synthetic_packets` pattern. Off-origin test boxes must keep `IO::out_{x,y,z}line_*` inside the domain; CarpetX/AMReX rejects domains < 8 cells per direction. Harness discovery: a `test/<name>.par` is only runnable once its golden dir exists; the first run against an empty golden dir is the capture step.
- Binding anti-patterns: never use AMReX's stateful RNG (`amrex/Src/Base/AMReX_Random.H`, [MCNX-RNG-05]); never adopt CarpetX's AoS/host-loop interpolator (`CarpetX/CarpetX/src/interp.hxx:38`, [MCNX-GPU-07]/[MCNX-CTX-07]). Grid access via the `ghext` relative-include idiom proven in `mcnux_cadence.cxx` (`#include "../../../CarpetX/CarpetX/src/driver.hxx"`); ccc grid functions written via `loop_all_device<1,1,1>` from `loop_device.hxx` (`mcnux_srcterms.cxx`); `REQUIRES CarpetX` alone suffices — never add `REQUIRES AMReX`. Contributor idiom for source terms: schedule IN the empty group `MCNuX_AddToSourceTerms` with a self-accumulating READS+WRITES(everywhere) pair, `+=` only ([MCNX-HYD-03]).
- Grid tallies: unordered atomic accumulation, no bitwise cross-rank/device assertions ([MCNX-RNG-08]) — review-check on every deposition task.
- The Cactus regression harness (flesh) resolves only inside the agent sandbox (`agent_scripts/sandbox/setup.sh`); harness-dependent tests run there, not in the analysis environment. Non-harness gates follow the `agent_scripts/gate_paramcheck.sh` two-leg exact-criterion pattern (transient output under `$CACTUSX/GATE/mcnux/`, never in the repo) and get CI wiring per the existing `ci.yml:163-164` precedent; `MCNXTHORNLIST` (`agent_scripts/build.sh:15,18`) is the hook for variant ThornLists.
- Consistency sweep (`specs/conventions-and-units.md:132`) forbids duplicated constant literals — standing review gate; currently clean (`mcnux_units.hxx` is the sole physical-constant literal site; `trpfix_*`/`srcfix_*`/`detail::pi` are fixtures/math, not violations).

## Tier 1 — Transport event chain (critical path)

- [ ] T18a-core: Interaction-time draw + episode-competition pure functions — `Δt_{s,a} = −ln(1−u_{s,a}) p^t/(κ_{s,a} ν)`; `ν = −p_μ u^μ` in MeV; shorter-of firing vs cell exit and Δt_rem; zero-opacity `+∞` sentinel, no NaN; tie-break to scattering; pinned `(k=0..3)` draw order, undrawn-remainder discard, e incremented per episode ([MCNX-INT-01/02/06]) — pure functions against synthetic inputs, unblocked now
  - spec: specs/neutrino-matter-interactions.md
  - tests: new `mcnux_selftest_interactions.cxx` rows — draw formula vs closed form (machine tier), sentinel/no-NaN fixture, tie rule, ν-computation identity (flat and pinned curved fixtures), k-order/e-increment policy on synthetic episodes; compile-time static_asserts in the new header aggregated via `stub.cxx`; SIZE bump + `unit-selftest` golden recapture.
  - notes: spec anchors `:83-126,165-179`; coefficients exclusively via T12/T10 interface, cell-centered ([MCNX-INT-05]) — never re-derived. Preserve spec-normative deviations verbatim (per-cell episode redraw, frozen episode kinematics, tie→scattering, spec `:249-271`). This plan run — dependency previously unlisted: the cell-exit-time source is T15's pusher; build the competition logic as pure functions against synthetic inputs first (testable via battery rows before integration), integrate once T15 has landed; `interactions-fixedseed` also needs T7 hydro data; draw slots `k=0(u_s),1(u_a),2,3`. SPLIT this plan run: this item is the pure core (T15 is landed, so it is fully unblocked); the scheduled half is T18a-int. No ν implementation exists; nearest building block is `metric_dot` (`mcnux_tetrad.hxx:187-194`); κ enters in geometrized units via `length_code_to_cgs = 1.476625038e5` (`mcnux_units.hxx:228`). Second run 2026-08-24: home the pure functions in a new `mcnux_interactions.hxx` (flat, `namespace MCNuX`, `#include`d from `stub.cxx`); opacity conversion factor also cited at `mcnux_units.hxx:147`; this is the first real `u(S,q,e,k)` consumer.
- [ ] T33: Fluid-state/four-velocity gather — extend MCNuX's runtime inputs to HydroBaseX: `INHERITS: HydroBaseX` (or explicit cross-thorn READS — see the T15 INHERITS decision) in `MCNuX/interface.ccl`, a cell-centered (ρ, T[MeV], Yₑ) + velocity→u^μ gather producing `FluidState` (`mcnux_coefficients.hxx:77-85`) at a packet's cell, with schedule READS declarations
  - spec: specs/neutrino-matter-interactions.md
  - tests: selftest rows for gather exactness on constant/linear synthetic fields (pattern: `mcnux_selftest_geodesic.cxx` rows 79-85) + a harness leg reusing the `hydro-sphere-id` TestMCNuX profile (piecewise (ρ,T,Yₑ) closed form, `TestMCNuX/src/hydro_profiles.cxx:39-86`) with a golden diagnostic of gathered values, parity 1e-12.
  - notes: new this plan run — hard structural blocker flagged by the interactions slice: no spec names the provider, but the input contract of [MCNX-INT-01/04/05] (`specs/neutrino-matter-interactions.md:71,156-163`) and [MCNX-PKT-04] requires cell fluid state and u^μ; `MCNuX/interface.ccl:13` currently inherits ADMBaseX only, and `VertexMetricGather`/`MetricSnapshot` (`mcnux_geodesic.hxx:59-66,240-339`) carry no hydro fields. Prerequisite for T14b, T18a-int, T18c (ν and tetrad both need u^μ; coefficients need cell fluid state). The T15 Decisions record (CST rejects bare cross-thorn READS) predicts `INHERITS: HydroBaseX` is required — if so, every MCNuX parfile must then activate HydroBaseX; update the parfile standing fact and existing parfiles in the same iteration. Unit note: HydroBaseX temperature vs the MeV convention (`specs/conventions-and-units.md`) — conversion happens in the gather, constants only from `mcnux_units.hxx`.
- [ ] T14a: Emission pure functions + `E_p` parameter (core half of former T14) — count law `N_p = g_s √−g ΔV Δt η_b(s)/E_p` with floor/Bernoulli remainder, g=4 applied exactly once at creation via `MCNuX::degeneracy` ([MCNX-PKT-02]); bin-center energy, weight `N=E_p/ν`, uniform position/time draws ([MCNX-PKT-03]); pinned `(q,e,k)` creation draw map (cell-level Bernoulli at `(q=K_cell, e=n, k=3b+s)`, then k=0..5 per packet) with `K_cell` bit packing ([MCNX-PKT-05]); `E_p` parameter in `param.ccl`
  - spec: specs/packet-representation-and-sampling.md
  - tests: new `mcnux_selftest_emission.cxx` rows for count-law floor/Bernoulli arithmetic at pinned fixtures, weight/bin-center assembly, `K_cell` packing bit-exact fixtures, draw-map k=0..5 order; static_asserts; SIZE bump + `unit-selftest` golden recapture.
  - notes: uses T12 analytic η; direction draw consumes T13; anchors spec `:90-162`, `specs/particle-container-and-gpu.md:180-188`. Builds T26's machinery with it. Spec anchor widened to `:90-170` ([MCNX-PKT-06] included); `degeneracy` at `mcnux_units.hxx:180,188-189`; the cell-key capacity assumption (patch<128, level<256, extent<65536, spec `:230-248`) has no guard — add a cheap runtime assert; packet-count control beyond `E_p` stays spec-deferred (`:245-248`). This plan run: no `E_p` parameter exists — add to `param.ccl` following the `opacity_source`/`kappa_a0` STEERABLE=NEVER idiom (`param.ccl:56-86`); direction draws call `mcnux_tetrad.hxx:255-323` with `u(S,q,0,4)`/`u(S,q,0,5)`, never literals; coefficients only through `evaluate_coefficients` (`mcnux_coefficients.hxx:145-153`); assemble `√−g = α√γ` from `spatial_metric_inverse().det` + gathered alpha (`mcnux_tetrad.hxx:105-116`, `mcnux_geodesic.hxx:240-339`). SPLIT this plan run (second 2026-08-24 run) into T14a (this pure core) and T14b (scheduled routine + benchmarks). BLOCKED DECISION carried from prior run: the count law consumes bin-integrated `η_b(s)` (MeV cm⁻³ s⁻¹) but the coefficient layer only returns per-point spectral `eta(s,E)` (MeV cm⁻³ s⁻¹ MeV⁻¹, `mcnux_coefficients.hxx:127-134`, `mcnux_table_coeffs.hxx:190-221`); no energy-bin grid exists anywhere — the implementer records the resolution (implementation freedom vs spec clarification to `packet-representation-and-sampling.md`) in Decisions before or within this task.
- [ ] T14b: Emission scheduled routine + `emission-fixedseed`/`stats-emission` benchmarks (integration half of former T14) — per-cell creation loop scheduled inside `MCNuX_TransportStep`, packet state written onto the T8 container schema ([MCNX-PKT-01]); production id generation via `idcpu`, unique across run/ranks, bit 63 never set ([MCNX-GPU-04] creation leg)
  - spec: specs/packet-representation-and-sampling.md
  - tests: `emission-fixedseed` benchmark — exact per-packet event/state sequence at pinned seed, single rank, golden parity 1e-12; `stats-emission` — count/energy/species estimators within 4σ at `N_p=2^20`, seed `1296518744`, archived z-values then golden-frozen at 1e-12 (no seed-shopping on a false 4σ failure); draw-map audit trace (exact); id-contract test (bit 63, uniqueness); `mcnux_packet_diag` per-packet observables verified.
  - notes: reuses T15's runtime container construction; the coverage matrix (`specs/verification-suite-design.md:399`) homes [MCNX-GPU-04]'s bullet in `interactions-fixedseed` — T14b carries the creation leg, T18b closes it with retirement. This plan run: only the synthetic 8-packet fixture constructs packets (`mcnux_geodesic.cxx:270-333`, manual `SetParticleIDandCPU(i+1,0)` at `:319`); needs a new cell-iteration idiom (existing `for_each_packet_tile` walks packets, not cells; append via the pinned-tile pattern, precedent `mcnux_geodesic.cxx:302-325`). Open decision the implementer records in Decisions: id-generation mechanism for real emission (fixture's manual `SetParticleIDandCPU` pattern vs container-tracked generation for cross-rank uniqueness — spec pins only the result). Depends on T33 (fluid state for η) and T14a; z-scores via `zscore()` (`mcnux_stats.hxx:77-95`); first statistical benchmark exercising [MCNX-RNG-07] against real MC estimators; parfiles + golden dirs under `MCNuX/test/`, capture step per harness discovery rule.
- [ ] T18a-int: Episode integration into `MCNuX_TransportStep` — wire the T18a-core competition into the transport step alongside/inside the geodesic push; per-cell episode boundaries (creation/step-start, post-scatter, cell-crossing — normative deviation from papers, golden-determining); cell-exit-time bookkeeping; `event_counter` incremented per the episode-boundary rule
  - spec: specs/neutrino-matter-interactions.md
  - tests: `interactions-fixedseed` deterministic benchmark (parfile + golden, `specs/verification-suite-design.md:244`); draw-map audit trace — record/compare `(q,e,k)` tuples consumed (spec `:215-217`; no scaffolding exists); interim selftest rows for episode-restart bookkeeping.
  - notes: spec anchors `:83-126,165-179`; coefficients exclusively via T12/T10 interface, cell-centered ([MCNX-INT-05]) — never re-derived. Preserve spec-normative deviations verbatim (per-cell episode redraw, frozen episode kinematics, tie→scattering, spec `:249-271`). SPLIT this plan run from T18a (pure core = T18a-core); depends on T14 packets + T18a-core. Cell-exit time exists nowhere (`mcnux_geodesic.hxx` has no notion of it) — whether the pusher or the interaction code derives it is implementation freedom (spec `:236-239`); implementer records the choice in Decisions. The `event_counter` schema slot exists (`mcnux_particles.hxx:63-68`); the unconditional exemplar increment in `exemplar_packet_kernel` is compile-only dead code — remove or wire it here. `interactions-fixedseed` also needs T7 hydro data. Second run 2026-08-24: also depends on T33 (fluid gather supplies u^μ and cell fluid state); the scheduled packet path to extend is `MCNuX_GeodesicPush` (`mcnux_geodesic.cxx:347-379`).
- [ ] T18b: Absorption — full packet removal (no weight decay), full-content transfer to T17 ledger via `absorption_delta`, id retirement (never reused) ([MCNX-INT-03]; [MCNX-GPU-04] removal leg)
  - spec: specs/neutrino-matter-interactions.md
  - tests: `stats-beam` — beam attenuation e^{−κΔ} within 4σ; discreteness check (weights never decay); ledger transfer sums exact per event; [MCNX-GPU-04] removal leg in `interactions-fixedseed` — retired ids never reappear (closes the coverage-matrix bullet at `specs/verification-suite-design.md:399`); id-contract runtime test (uniqueness/no-reuse/immutability/bit-63 over a multi-step run with creation and removal, `specs/particle-container-and-gpu.md:255-257` — becomes buildable once T14b+T18b land).
  - notes: part of the `interactions-fixedseed` golden benchmark (`specs/verification-suite-design.md:244`, needs T7 hydro data). Ledger arithmetic exists (`mcnux_srcterms.hxx:72-78`, already unit-tested), the event side does not; spec anchor `:128-135`; needs T18a. This plan run: no removal path exists anywhere (no `RemoveParticleAtIndex`/compaction in the codebase — this is the first packet-removal path); wiring precedent for the ledger deposit is `MCNuX_SyntheticDeposit` into `MCNuX_AddToSourceTerms` (`mcnux_srcterms.cxx:82-140`, `schedule.ccl:159-178`); contributors `+=` only; deposit via `absorption_delta` + `source_from_delta` (`mcnux_srcterms.hxx:71-87`); feeds T19's real-closure leg.
- [ ] T18c: Elastic scattering — isotropic fluid-frame redraw at fixed ν via T13 tetrad (`fluid_frame_direction`/`fluid_momentum_to_coordinate`, k=2/3 drawn by the caller); momentum-change into ledger via `scattering_delta` ([MCNX-INT-04])
  - spec: specs/neutrino-matter-interactions.md
  - tests: `stats-scatterbox` — isotropy + elasticity (ν preserved, machine tier) within 4σ; fixed-seed event-sequence parity; selftest rows (elasticity, isotropy fixture).
  - notes: reuse T13's tetrad — never reimplement (spec `:136-154`). Primitives exist (`mcnux_tetrad.hxx:239,290`, `mcnux_srcterms.hxx:79-84`), event missing; per-episode tetrad caching is spec-permitted (`mcnux_tetrad.hxx:178`); needs T18a. This plan run: deposit `N(p^μ_out−p^μ_in)` via `scattering_delta` (`mcnux_srcterms.hxx:81-87`); tetrad machinery at `mcnux_tetrad.hxx:206-323`. Second run 2026-08-24: also needs T33 (u^μ for the fluid frame); weight/species unchanged; per-episode tetrad/coefficient caching has no precedent — record the choice in Decisions; prerequisite for the diffusion entry criterion (T22b triggers "after every performed scattering").

## Tier 2 — Unblocked benchmarks & audits

- [ ] T16: `schwarzschild-pt` benchmark — parfile + golden data: packet orbits on T6's Schwarzschild background, `p_t = −α²p^t + β^ip_i` tracked along the trajectory, drift ≤1e-5 at `h=M/16`, `T=100M` ([MCNX-GEO-05])
  - spec: specs/geodesic-propagation.md
  - tests: the benchmark itself (explicit numeric tolerance pinned per benchmark) + convergence order ≥2 across Δt, Δt/2, Δt/4 + golden parity 1e-12.
  - notes: depends on T6 + T15; separate build+test cycle from both; anchors spec `:117-132`, `specs/verification-suite-design.md:241-242`. The 1e-5 drift bound is a design bound, re-pinnable at golden-creation time per spec `:456-493`. Nothing computes `p_t` along a trajectory today (only the null-closure form exists); this is the first parfile activating `TestMCNuX` — it must live under `MCNuX/test/*.par` ([MCNX-BLD-06]). T15 landed: reuse `test_synthetic_packets` fixture + `mcnux_packet_diag` (`pk_pt` column) as the observable; the trilinear gather's derivatives are first-order on curved metrics — the 1e-5 drift bound at h=M/16 may need a higher-order gather (swap `VertexMetricGather` only; `geodesic_step` is templated on the gather) or the spec's Open-questions fallback. This plan run — fully unblocked (T6+T15 both done): packets at isotropic r∈[6M,10M] (benchmark contract `specs/verification-suite-design.md:242,360-364`); the fixture's |x^i|≤0.25 positions are NOT reusable — add a fixture leg placing packets at r∈[6M,10M] (the seeding mechanism is reusable; the existing `PacketFixture` at `mcnux_geodesic.cxx:263-284` is Minkowski-specific — reuse-vs-new seeding routine is an implementer decision); domain must avoid the r<M/2 isotropic-writer singularity (follow the `schwarzschild-id.par` offset-box idiom); ActiveThorns needs CarpetX IOUtil ODESolvers ADMBaseX HydroBaseX MCNuX TestMCNuX; do NOT pre-build the higher-order gather — it is contingent on this benchmark's results (spec escape hatch `:177-179`, fallback `:185-195`).
- [ ] T19: Conservation-ledger closure check — per-step audit: event-side sums `Σ_events δX` vs grid totals `Σ_cells(source)·ΔV·Δt` for energy/momentum/lepton number (all five channels), relative 1e-13/step with an `ε₀` floor for the all-zero case; test-configuration-only allowed; escape tallies (per-species energy/number at outer boundary, README boundary policy) belong to this ledger surface ([MCNX-HYD-05])
  - spec: specs/hydro-coupling-source-terms.md
  - tests: closure to 1e-13/step in fixed-seed benchmarks (T14/T18 configurations), asserted as a golden pass flag; the skeleton leg runs now against the synthetic deposit fixture.
  - notes: depends on T17 + T18; spec anchors `:177-188,295-296`. Synthetic-event arithmetic validation can land with/after T17; real closure needs real producers. Always-on vs test-only audit computation is an implementer decision (spec `:295-296`). No audit/escape code exists; the implementer must also record the escape-tally home (new MCNuX-owned grid variables vs audit-internal bookkeeping) in the Decisions section. This plan run: spec explicitly permits a test-configuration-only build against the synthetic fixture (`:295-296`), so the audit skeleton can land BEFORE T18b/T18c and be extended when real events exist; escapees are currently dropped by AMReX untallied (`mcnux_geodesic.cxx:344-346`; [MCNX-GPU-05] requires "removed and tallied as escapes"). Second run 2026-08-24: the untallied drop also violates `geodesic-propagation.md:66-69` (per-species energy/number escape tallies); escape-tally MECHANICS (where removal is detected) live with T23, arithmetic/home decision here — do not implement twice; spec ownership among geodesic-propagation / hydro-coupling / particle-container defers to `specs/README.md` boundary policy — check it when scoping.
- [ ] T20: `cadence-lag` benchmark — golden benchmark demonstrating source terms advance exactly once per coarsest Δt with the one-step lag semantics ([MCNX-CTX-05] verification): step-function fluid perturbation at iteration n first affects source terms at n+1
  - spec: specs/verification-suite-design.md
  - tests: golden parity 1e-12 (counter + source-term tally checksum TSV); counter increments once per CCTK_EVOL iteration, not per RK stage, with RK4 active; checkpoint/recovery leg reproduces the sequence.
  - notes: depends on T4 + T14/T17; spec anchor `:238-247`. Counter half already exists (`transport_step_count` in `MCNuX/interface.ccl`). This plan run: T4/T17 are done, making the old dependency line ambiguous — the synthetic-deposit fixture (`test_synthetic_deposit`) plausibly suffices as the step-function perturbation source without waiting for T14 emission; attempt with the synthetic fixture first, defer behind T14 only if the observable proves vacuous (record the outcome in Decisions). Counter at `mcnux_cadence.cxx:74-89`, zero-then-add scaffold at `schedule.ccl:100-112`; [MCNX-CTX-05] anchor `specs/carpetx-thorn-integration.md:196-203`. Second run 2026-08-24: the counter output is currently log-only — the benchmark needs golden-diffable output (the scalar is a grid scalar; TSV via IO), and the spec's checkpoint/recovery leg has no artifact yet.
- [ ] T30: VER-03/VER-04 closed-form selftest leg — unit-selftest rows recomputing the Schwarzschild (ψ, γ_ij, α) and hydro-profile (ρ, T, Yₑ) closed forms at pinned points plus the identities `α(1+M/2r)=1−M/2r` and `γ_ij=ψ⁴δ_ij`, each at 1e-14 relative
  - spec: specs/verification-suite-design.md
  - tests: new `mcnux_selftest_testmcnux.cxx` appender rows (append-only; bump SIZE + regenerate golden TSV); identities at machine tier.
  - notes: new this plan run — spec anchor `:315-319,412-413`; today only the 1e-12 golden diff of writer output exists (spot-checked once manually at capture, `schwarzschild-id.par:14-16`). Design ambiguity the implementer records in Decisions: host-side closed-form recomputation (matches the `coef.analytic.*` precedent, no design change — recommended) vs reading back grid data from an activated TestMCNuX run (literal spec wording "evaluates the TestMCNuX writers' outputs" — would force activating TestMCNuX+ODESolvers in `unit-selftest.par`, a design change). Second run 2026-08-24: re-confirmed still absent (no `mcnux_selftest_testmcnux.cxx`, SIZE still 106).
- [ ] T31: Production table-residency layer + table-mode benchmark — load the production `.h5` tables (EOS, EmAb, Iso baseline) into device-resident storage, build real `BaselineTableViews`, consumed only through `RangedTableCoefficients`; first end-to-end `opacity_source = "table"` run
  - spec: specs/opacity-eos-evaluation.md
  - tests: kernel-parity selftest legs against the real tables (strengthens [MCNX-OPA-01]'s "identical inputs and table data" claim, currently synthetic-only); a `test/` parfile + golden with `opacity_source = "table"`, pinned (ρ,T,Yₑ,E) probe points golden-checked at 1e-12, plus clamp-counter/transparency-floor observability on out-of-range probes.
  - notes: new this plan run — implementation-freedom item `specs/opacity-eos-evaluation.md:325-330`; all table evaluation code is landed and selftested against synthetic in-memory tables only; `opacity_source` defaults to `"table"` (`param.ccl:56-60`) yet all four parfiles pin `"analytic"` — table mode has never run end-to-end. Also the natural home for [MCNX-OPA-07] hard-failure enforcement once a call site invokes inversion (`inversion_usable` predicate exists, `mcnux_table_range.hxx:226-229`; no consumer). Sequencing: needed before any table-mode equilibration benchmark (T29-class), not before T14–T18 in analytic mode. Second run 2026-08-24: concrete table set is `wl-EOS-SFHo-15-25-50.h5` + `wl-Op-SFHo-15-25-50-E40-{EmAb,Iso}.h5` into `wli::TableView`s populating `BaselineTableViews`; deferral self-documented at `mcnux_table_coeffs.hxx:69-72`; owning-spec ambiguity — `opacity-eos-evaluation.md:40-42` points at `particle-container-and-gpu.md` for "table residency mechanics"; confirm before anchoring requirement ids (see Decisions).

## Tier 3 — Trapped regime & equilibration

- [ ] T21b: α selection + TRP parameter surface + diagnostic — pure `alpha_select(kappa_a, dt_c, xi) → min(1, ξ/(κ_a Δt_c))`, fixed-α override parameter, `κ_a′Δt_c ≤ ξ` per-cell diagnostic; `MCNuX/param.ccl` entries for ξ (default 1), τ_diff (default 3), scheme selector explicit/relabeled (names free, semantics binding, spec `:434-436` — param.ccl currently has zero TRP entries) ([MCNX-TRP-04])
  - spec: specs/trapped-regime-treatment.md
  - tests: selection-rule selftest rows (exact) incl. α=1 branch and override precedence; golden regen.
  - notes: bitwise-limit benchmark needs T18; α-selection anchor `:145-171`. SPLIT so the unblocked parameter/selection half can land now (`trp-alpha-limit-*` benchmarks are T21c); consumes `MCNuX::relabel()` — never re-author; the implementer records whether the diagnostic is a battery check or a runtime grid scalar (spec `:169,370-372`, freedom `:421-439` — affects whether `interface.ccl` gains a variable). This plan run: `mcnux_trp.hxx:36-41` self-documents the deferral; append rows to `mcnux_selftest_trp.cxx` (currently rows 66-70); follow the `opacity_source` idiom for the scheme-selector KEYWORD; no per-cell diagnostic precedent exists in `interface.ccl`. Second run 2026-08-24: also add α∈(0,1] paramcheck enforcement for the fixed override (spec assumption, not yet built), with a paramcheck-gate leg for out-of-range ξ/α.
- [ ] T22a: TRP diffusion kinematics pure functions (standalone math half) — `p_ns = exp(−κ_s′Δt′_rem)`, truncated-Gaussian displacement branch selector (`P < 1−p_ns`) + closed-form antiderivative `F` inversion (≤1e-12 in r̃), causal cap `r_d ≤ Δt′_rem` ([MCNX-TRP-08]); correlated momentum θ₂ draw with 21-point `B_i` table (`1000, 18.74, 7.52, …`) + linear interpolation, `cosθ₂ = B − (1+B)((B−1)/(B+1))^r`, atom-branch `cosθ₂=1` at `f_free=1` ([MCNX-TRP-10])
  - spec: specs/trapped-regime-treatment.md
  - tests: selftest rows — F/F⁻¹ round-trip ≤1e-12; branch-selector fixtures; `B_i` interpolation fixtures; atom-branch pin (exact).
  - notes: spec anchors `:219-321`. Split: pure scalar math is unblocked and testable now via battery rows; the integration leg (two-leg position update, [MCNX-TRP-09]) moved to T22b with the rest of the blocked dispatch work. Re-confirmed: grep-confirmed absent; anchors narrowed to `:219-247,279-321`. Second run 2026-08-24: the spec's recorded deviations/resolutions (coordinate-time split, cell-pinned coefficients/tetrad, 4-velocity advection restatement, criterion-after-every-scattering, no absorption draw during diffusion) are normative and determine the exact formulas — preserve verbatim.
- [ ] T21c: `trp-alpha-limit-{explicit,relabeled}` bitwise-limit benchmarks — α=1 relabeled run reproduces the explicit run BITWISE (events, population, golden) at fixed seed, single rank ([MCNX-TRP-05]; [MCNX-TRP-01] explicit baseline is verified here — no standalone artifact by design)
  - spec: specs/trapped-regime-treatment.md
  - tests: α=1 run reproduces explicit run BITWISE at fixed seed, single rank (spec `:173-181`; `specs/verification-suite-design.md:247,375`), golden 1e-12.
  - notes: bitwise-limit benchmark needs T18; α-selection anchor `:145-171`. SPLIT from T21b; needs T14, T18, T21b; record explicitly that [MCNX-TRP-01] is folded into this pair. This plan run: requires wiring `relabel()` (`mcnux_trp.hxx:60-64`, currently zero consumers) into the coefficient-consumption site inside the event loop; the static α=1 identity is proven header-side only (`mcnux_trp.hxx:132-139`). Second run 2026-08-24: `relabel()` substitutes into the emission count law and event-sampling coefficient consumption "there and nowhere else" ([MCNX-TRP-02/03]); α-convention guard on every α-dependent diagnostic.
- [ ] T22b: TRP regime entry + episode dispatch + position/momentum integration + 5-draw map — `κ_s′Δt′_rem > τ_diff` evaluated after EVERY performed scattering (documented deviation, spec `:182-217,507-522`), one diffusion episode per (packet, step), no absorption channel inside; two-leg position update (advection leg `x^i ← x^i + (u^i/u^t)(1−f_free)Δt_rem` + free-streaming leg via T15's pusher, [MCNX-TRP-09]); tetrad-transformed momentum outcome via T13; fixed five-draw order `k=0..4` (P, cosθ_f, φ_f, r, φ₂) ([MCNX-TRP-06/07])
  - spec: specs/trapped-regime-treatment.md
  - tests: `stats-diffusion` (three-clause, 4σ via T26); `trp-overlap-consistency`; scheme identities; diffusion-leg draw-map audit in `interactions-fixedseed` (spec `:361-406`).
  - notes: depends on T18. Also needs T13, T15, T22a. Preserve the spec's four logged deviations verbatim (`:443-557`). Also needs T21b (scheme selector, τ_diff); anchors `:182-217,249-277,323-339`; Eq. 50 β-dependent bound remains spec-deferred (`:450-455`).
- [ ] T29: `stats-equilibration-{explicit,relabeled}` benchmarks — equilibration-box configuration driving the full emission/absorption/scattering chain to thermal equilibrium; arbiter of OPA's stimulated-absorption and chemical-potential conventions (`specs/opacity-eos-evaluation.md:338-379`) ([MCNX-VER-07])
  - spec: specs/verification-suite-design.md
  - tests: 4σ-then-golden protocol via T26 machinery at `N_p=2^20`, seed `1296518744`; each variant documents its own σ formula from its governing leaf spec (`:255-279`).
  - notes: no prior TODO owner before last plan run. Needs T7 (equilibration box), T10 (table coefficients), the T14/T18 chain, and T21a/T21b for the relabeled variant. Reuse T26 — no new statistical machinery. Carried forward unchanged; relabeled variant also needs T21c's bitwise-limit pair as its baseline. This plan run: table mode additionally needs T31 (production table residency) — the equilibration box is the first benchmark that cannot run analytic-only. Second run 2026-08-24: the TestMCNuX "equilibration box" hydro keyword is already implemented (`TestMCNuX/param.ccl:74-95`).

## Tier 4 — GPU delivery & optional physics

- [ ] T23: GPU redistribution + ownership invariants + scheduling-mode multi-box artifact — local `Redistribute`/`RedistributeLocal(N)` inside `MCNuX_TransportStep` after the last position-changing op (and after regrid), gated on bounded motion (N from a proven per-step motion bound, else global `Redistribute()`); after-step ownership invariant: every packet on the rank/level/grid/tile owning its position; escapees removed/tallied, never redistributed ([MCNX-GPU-05/06]); the same multi-box artifact proves exactly one observer invocation per (patch,level) traversal ([MCNX-CTX-03] verification, absorbs former T27b)
  - spec: specs/particle-container-and-gpu.md
  - tests: `ownership-multibox` harness benchmark (≥2 boxes, ownership after step, golden) runnable via `agent_scripts/test.sh`; bounded-motion audit; single-rank determinism check (bitwise-identical populations across two fixed-seed runs — rides on T14's fixedseed benchmark); golden `transport_step_count` output closing [MCNX-CTX-03] (`specs/carpetx-thorn-integration.md:209-211`).
  - notes: depends on T4 step loop + T15 motion; spec anchor `:190-209`; `specs/verification-suite-design.md:245`. Minkowski, ≥2 boxes per rank via small `CarpetX::max_grid_size` under `NPROCS 1`, packets crossing box boundaries; the "synthetic motion kernel to unblock earlier" option is not taken — T15 supplies the real bound; redistribution under subcycling (spec `:308-311`) stays open and moot while `use_subcycling = no`. Former T27b notes carried in: [MCNX-CTX-03] was verified only manually during T4 (64-box rerun, 10 firings in 10 iterations) — not reproducible via `agent_scripts/test.sh` today; matrix row `specs/verification-suite-design.md:403-409`. This plan run: MERGED T27b into this task (same parfile family, one build+test cycle; synthesis recommendation); current state is an unconditional global `Redistribute()` (`mcnux_geodesic.cxx:374-375` — spec-compliant fallback, in-code comment marks it "T23 refines this"); the [MCNX-GPU-06] regrid-timing requirement is currently untestable by construction (all tests single-level 8³) — record it as an explicit non-goal or sub-scope decision; also decide whether permanent global `Redistribute()` is an acceptable spec-compliant endpoint (spec permits it absent a claimed bound). Second run 2026-08-24: no `OK()`-style placement assertion exists after any step; escape-tally MECHANICS land here (arithmetic/home decision in T19 — do not implement twice); no bitwise cross-rank grid-tally assertion permitted ([MCNX-RNG-08]).
- [ ] T25d: GPU-backend build of the pinned ThornList + device RNG purity leg ([MCNX-BLD-03/04], [MCNX-GPU-02], [MCNX-RNG-06] device leg)
  - spec: specs/build-and-integration.md
  - tests: GPU-backend configure+build green; precision gate static_asserts hold with AMReX headers; per-packet-operator device-kernel inspection; device-vs-host bitwise equality of `MCNuX::u` over the `mcnux_rng.hxx` `detail::purity_batch` tuples (exact).
  - notes: currently only a CPU/MPI cfg exists in the sandbox. CI compile-only cuda/rocm matrix already exists (`.github/workflows/ci.yml:173-247`); missing legs are the sandbox GPU harness run and the deferred host-vs-device RNG-purity check (deferral recorded in `mcnux_rng.hxx:32-40`; discovered 2026-08-14 during T2 — the CPU/MPI sandbox cannot exercise this leg, so T2 shipped with all host-side purity legs asserted at compile time). GPU-vs-golden norm tolerances explicitly undecided pending first GPU run (spec-recorded open). `agent_scripts/sandbox/setup.sh` builds AMReX CPU-only via mpicc/mpicxx with no `AMReX_GPU_BACKEND` — a GPU AMReX build + second `mcnux` configuration is part of this task; `purity_batch` at `mcnux_rng.hxx:382-457`; the KAT must hold host+device (`specs/verification-suite-design.md:346`); CI matrix self-describes as not correctness-bearing (`ci.yml:171`). This plan run: any claim of GPU-obligation "done" must land this task, not point at CI.
- [ ] T24: Optional Tmunu contribution — `REQUIRES TmunuBaseX` in `configuration.ccl`; parameter-gated (BOOLEAN, default off, gates storage + scheduling) routine `IN TmunuBaseX_AddToTmunu` (`CarpetX/TmunuBaseX/schedule.ccl:31-33`), MC stress-energy estimator (arXiv:1708.08452v2 Eq. 16 as restated in spec) with exact 8-point cell-to-vertex average `eT(v) += Σ_{d∈{0,1}³} q(v−d)/8`; writes only through TmunuBaseX's extension group ([MCNX-HYD-06/07])
  - spec: specs/hydro-coupling-source-terms.md
  - tests: deposition-identity unit test for the 8-point average (machine tier) + sign fixtures; gated-off default leaves eT* untouched (exact — off ⇒ bit-identical to baseline golden).
  - notes: deferrable; unresolved double-counting question when enabled with four-force sources on evolving spacetime — spec amendment required before that combined path; do not enable both in any committed parfile (spec `:190-222,330-336`). Never mark this and the four-force coupling simultaneously "done" without addressing the open question. Needs a packet population (T14); no cell-to-vertex code exists anywhere yet. This plan run: the implementer must not silently resolve the double-counting open question. Second run 2026-08-24: meaningful physics values only after real events (T18b/T18c); fixture-driven interim golden acceptable.

## Tier 5 — Gates & spec hygiene (unblocked, low urgency)

- [ ] T27a: Validity-machinery gate — gate script running an existing-machinery parfile (`source-zero-add.par` or `unit-selftest.par`) with `CarpetX::poison_undefined_values = yes` (clean pass) plus a deliberately under-declared READS/WRITES meta-test variant (test-only param-gated routine) asserting the driver's checksum/poison error fires (`specs/carpetx-thorn-integration.md:204-208`, [MCNX-CTX-04] verification)
  - spec: specs/carpetx-thorn-integration.md
  - tests: the gate itself is the test — exact-criterion pass/fail on both legs.
  - notes: currently satisfied by nothing — no parfile sets `poison_undefined_values`, no meta-test exists; needs no new physics. Precedent `agent_scripts/gate_paramcheck.sh`; planner decision taken: gate-now (spec treats the bullet as independent), CI wiring per precedent. The implementer records whether the "(and a presync-mode test run)" clause (`:205`) is already met by the standing `presync_mode = "mixed-error"` convention or needs an explicit leg.
- [ ] T25b: Configure-time negative-path gate — script building a `mcnux.th`-derived ThornList variant omitting `CarpetX/CarpetX` and/or `WeakLibInterp/WeakLibInterp` (reuse `MCNXTHORNLIST`) and asserting configure-time failure distinct from other failure modes ([MCNX-BLD-02] verification)
  - spec: specs/build-and-integration.md
  - tests: gate script's own pass/fail (exact); script location convention is implementation freedom.
  - notes: spec anchor `:186-189`; CI wiring per gate precedent. Variant lists committed under `agent_scripts/gates/` or generated in-script (implementer's choice, spec `:222-224`). Second run 2026-08-24: MERGED — this one script also covers [MCNX-CTX-02]'s configure-time verification (`carpetx-thorn-integration.md`); both requirement ids close here.
- [ ] T25c: Activation-split audit sweep + layout audit + grep gates — script asserting `TestMCNuX` appears in ActiveThorns only within `MCNuX/test/*.par` ([MCNX-BLD-06]) + smoke configure/build/start with a ThornList omitting `MCNuX/TestMCNuX`; fold in the layout-audit bullet (`specs/build-and-integration.md:203-206` — exactly two thorns, no `TestMCNuX/test/`, 13 spec files + validator), a grep gate failing on `AMReX_Random.H`/`AMReX_RandomEngine.H`/`ParallelForRNG` anywhere in `MCNuX/src/` ([MCNX-RNG-05] made mechanical), and a grep gate failing on `interp.hxx` inclusion in `MCNuX/src/` ([MCNX-CTX-07] made mechanical)
  - spec: specs/build-and-integration.md
  - tests: sweep script pass/fail (exact).
  - notes: relevant once parfiles exist (after T5/T14). Parfiles now exist; property currently holds by inspection. Planner decision taken: layout audit folds into this script rather than staying manual. Five parfiles exist; `schwarzschild-id.par` and `hydro-sphere-id.par` legitimately activate `TestMCNuX` under `MCNuX/test/` — the sweep asserts location, not absence; the no-`TestMCNuX` smoke build has never run (pinned `mcnux.th` always includes it — needs a second config or an `MCNXTHORNLIST` swap; implementer records the choice); CI wiring per precedent; the RNG-05 grep consolidation turns a review convention into a check. Second run 2026-08-24: added the [MCNX-CTX-07] `interp.hxx` grep leg (currently a review-time claim only; no fresh exhaustive grep was run this pass).
- [ ] T28: Spec-corpus text fixes — (a) amend `specs/conventions-and-units.md:129` (and the `specs/README.md:13` restatement), which demands `1e-14` relative agreement against the *pinned 10-digit* factor values — arithmetically impossible (truncated literals deviate from full-precision recomputation by up to 2.1e-10; worst: emissivity 2.06e-10); T1 resolved it per [MCNX-CNV-04] (extra digits allowed) with `rtol_pinned=1e-9` vs pinned literals and `rtol_machine=1e-14` for full-precision identities — the amendment states this two-tier tolerance so the `unit-selftest` criterion is satisfiable; (b) fix the validator invocation `bash tools/validate_specs.sh` → `bash specs/tools/validate_specs.sh` corpus-wide (`specs/README.md:103` + the Verification section of all 12 coded specs); (c) fix the stale "table layer is a later task" comment at `mcnux_coefficients.hxx:24-26` (the table layer now exists); (d) add the h_cgs traceability note (`h_cgs` is a 6th pinned constant governed by `specs/opacity-eos-evaluation.md:356-361`, not authorized by the conventions spec's own 5-constant table — record the cross-reference in `mcnux_units.hxx`/`specs/conventions-and-units.md`)
  - spec: specs/conventions-and-units.md
  - tests: `bash specs/tools/validate_specs.sh` exit 0 after the edit (exact).
  - notes: discovered 2026-08-14 during T1; low urgency — code already carries the rationale (commented in `MCNuX/src/mcnux_units.hxx`). Scope includes authoring the amendment via spec-author (no new spec file; edit in place); bundle both edits in one iteration. (b) is corpus-wide — every coded spec's Verification section carries the stale invocation, not just the README; fix all in the same iteration (text-only; `validate_specs.sh:10-30` does not inspect this string); the worked derivation for (a) is at `mcnux_units.hxx:35-48`. This plan run: re-verified 2026-08-24 — 14 stale occurrences remain (grep-confirmed), zero corrected ones; the seed hex-gloss is NOT part of this task (already fixed at T26, verified in place — see Decisions). Second run 2026-08-24: fold-ins (c) and (d) added from the cross-cutting sweep; all four edits are non-functional and bundle in one iteration.
- [ ] T32: Two-contributor split meta-test — split deposition across two artificial contributors scheduled `IN MCNuX_AddToSourceTerms` and prove the result equals the single-contributor run to floating-point-reduction tolerance ([MCNX-HYD-03] verification)
  - spec: specs/hydro-coupling-source-terms.md
  - tests: harness or gate parfile pair comparison.
  - notes: new this plan run — spec anchor `specs/hydro-coupling-source-terms.md:248-253`; distinct from T27a's poison gate; no artifact exists; natural piggyback on the synthetic-deposit fixture (`mcnux_srcterms.cxx:82-140`).

## Decisions & non-blocking spec items

- RESOLVED at T15 — INHERITS vs bare READS: the CST rejects bare cross-thorn `READS: ADMBaseX::…` ("No access to variable 'ADMBASEX::gxx' … Maybe add an inherits clause"), so `INHERITS: ADMBaseX` lives in `MCNuX/interface.ccl`; consequence: ADMBaseX must be active in every MCNuX parfile (tests + gates updated). `READS: ...(everywhere)` on the ADMBaseX groups passes the driver's presync validity at every iteration. T33 will likely repeat this for HydroBaseX — if so, HydroBaseX joins the every-parfile activation list.
- RESOLVED at T6 — cross-repository `EXTENDS KEYWORD` works: the CST accepts `SHARES: ADMBaseX` + `EXTENDS KEYWORD initial_data`/`initial_lapse` from a sibling-arrangement thorn and the extended parfile value is accepted at run time; the documented fallback (`specs/verification-suite-design.md:465-466`, own keyword + `"none"` base values) is unneeded. T7 confirmed the same for `EXTENDS KEYWORD initial_hydro` on `HydroBaseX`.
- RESOLVED at T4 — PARAMCHECK vs schedule-tree ordering: the flesh silently ignores an unresolvable `AFTER ODESolvers_Solve` (no schedule-resolution error precedes the T3 paramcheck abort; verified both with ODESolvers inactive and with `use_subcycling=yes` where only `ODESolvers_Solve_Subcycling` exists). Caveat: with the AFTER target absent the group's placement in `[CCTK_EVOL]` is arbitrary (observed *before* `ODESolvers_Solve_Subcycling`) — T3's guard is therefore load-bearing for [MCNX-CTX-05], not merely defensive; any future subcycling extension must revisit the ordering clause, not just drop the guard.
- RESOLVED at T26 — primary-seed hex gloss: `specs/rng-and-statistical-acceptance.md:104` glossed seed 1296518744 as 0x4D434E58, but that hex equals 1296256600; the decimal literal governs, true hex is 0x4D474E58 — gloss fixed in place by spec-author, validator green. Re-verified 2026-08-24 by direct grep (the spec now reads `0x4D474E58`) after one findings file misreported it as still unamended — the fix IS in place; do not re-open. A deliberate re-pin to the ASCII-"MCNX" value 0x4D434E58 remains available to a future planner (would change spec decimal + validator pin + `mcnux_stats.hxx` constants + battery row 105 together; golden data unaffected while no draws consume the seed), but is NOT proposed.
- RESOLVED at T8 — header/gate permanent home: `stub.cxx` does not retire; each shared header carries its own compile-time selftests (AMReX precision-gate legs live in `mcnux_particles.hxx`), `stub.cxx` keeps the plain double/CCTK_REAL legs and remains the aggregation point including every shared header.
- RESOLVED — `.gitignore` build products unnecessary: multiple sandbox builds completed with the repo staying clean (build lives in mounted `$CACTUSX` tree per `agent_scripts/sandbox/README.md:39`).
- Gate CI wiring: CLAUDE.md calls gates "sandbox only" but `gate_paramcheck.sh` already runs in CI (`ci.yml:163-164`) — precedent decided: new gates (T27a, T25b, T25c, T23's artifact if gate-shaped) get CI wiring too.
- MERGED (prior run) — former T27b (scheduling-mode multi-box artifact, [MCNX-CTX-03]) is absorbed into T23: same multi-box parfile family, one build+test cycle; its notes are carried inside T23. MERGED (this run) — [MCNX-CTX-02] configure-time verification folds into T25b: one gate script covers both it and [MCNX-BLD-02].
- SPLIT (this run) — T14 → T14a (pure core + `E_p` param) and T14b (scheduled routine + benchmarks), mirroring the T18a-core/T18a-int precedent; new T33 (fluid-state/four-velocity gather) created as the interaction chain's structural input provider — no spec names the provider, so its requirement anchors are the INT/PKT input contracts, not a new requirement id.
- η_b(s) bin-integration (T14a's blocked decision) is NOT yet confirmed as a spec defect — no spec-author dispatched this run; the T14a implementer either resolves it as implementation freedom (recording the bin-grid choice) or escalates it as a `packet-representation-and-sampling.md` clarification for the next plan run.
- Sanctioned debt, untracked: the relative-include TODO comments (`mcnux_cadence.cxx:18`, `mcnux_geodesic.cxx:19`) are the accepted CLAUDE.md idiom — CarpetX offers no alternative header; do not task their removal.
- Not tasked: a compile-time guard tying the selftest row count to `interface.ccl` SIZE (runtime CCTK_VERROR only, `mcnux_selftest.cxx:229-237`) — not spec-required; revisit only if drift ever occurs.
- Scripted no-duplicate-literal sweep joining `validate_specs.sh` vs staying a manual review gate — tooling scope, not blocking; revisit if a violation ever slips through review. (The [MCNX-RNG-05] and [MCNX-CTX-07] greps, by contrast, are folded into T25c.)
- T4 recovery caveat (not exercised): if CarpetX doesn't mark recovered scalars valid under mixed-error, schedule the init routine also `AT CCTK_POST_RECOVER_VARIABLES`.
- Cheap unverified assumptions flagged this run (one-command sandbox checks before relying on them): the 106-column shape of the `unit-selftest` golden TSV was not independently re-verified, and `specs/tools/validate_specs.sh` was read but not executed this pass.
- Implementer-recorded choices pending: T14a η_b(s) bin-integration path (see the standalone bullet above); T14b id-generation mechanism (manual `SetParticleIDandCPU` pattern vs container-tracked cross-rank generation); T18a-int cell-exit-time ownership (pusher vs interaction code); T18c per-episode tetrad/coefficient caching; T20 fixture sufficiency (synthetic deposit vs real emission as the perturbation source); T27a presync-mode clause reading (explicit leg vs satisfied by `mixed-error`); T19 escape-tally home (grid variables vs audit-internal), always-on vs test-only audit, and escape-tally spec ownership per `specs/README.md` boundary policy; T21b diagnostic as battery check vs runtime grid scalar; T30 host-side recomputation (recommended) vs writer-output readback; T16 curved-space fixture reuse-vs-new seeding routine; T23 global-`Redistribute()` endpoint acceptability and regrid-timing sub-scope; T25c separate Cactus config vs `MCNXTHORNLIST` swap for the no-`TestMCNuX` smoke build; T25d GPU-vs-golden norm tolerances (spec-undecided until the first GPU run); T31 owning spec for table-residency mechanics (`opacity-eos-evaluation.md` vs `particle-container-and-gpu.md` per the latter's citation).
- [MCNX-GPU-04] is verified across two tasks by design: creation leg (bit 63 clear, uniqueness) in T14b, removal/retirement leg in T18b, which closes the single coverage-matrix bullet (`specs/verification-suite-design.md:399`). [MCNX-TRP-01] has no standalone artifact by design — T21c's bitwise-limit pair is its verification.
- `source-zero-add` is an extra benchmark outside the pinned [MCNX-VER-06] set (permitted, `specs/verification-suite-design.md:449-450`); no matrix row required.
- Explicitly spec-deferred (NOT deliverables): packet-count control beyond `E_p` (`specs/packet-representation-and-sampling.md:245-248`); β-dependent Eq. 50 stability bound (`specs/trapped-regime-treatment.md:450-455`); NES/Pair/Brem νx thermal-emission assembly (`specs/opacity-eos-evaluation.md:340-349`); HYD-06 simultaneous-use double-counting (`specs/hydro-coupling-source-terms.md:330-336`); redistribution under subcycling (`specs/particle-container-and-gpu.md:308-311`).

## Discovered since last plan
