# TODO — MCNuX build plan

Foundations and core enablers are in: T1–T5 + T25a (units header, Philox RNG header, subcycling paramcheck guard + gate, transport cadence group, `unit-selftest` battery, spec-corpus validator green) and the Tier-1 enablers T8 (pure-SoA `PacketContainer`), T9 (WeakLibInterp wrappers), T13 (tetrad + null closure), T17 (source-term grid variables + zero-then-add + `source-zero-add` test), T21a (TRP relabeling map). Plan run 2026-08-23 re-verified every slice against live code: no drift, no stale anchors, nothing partially landed beyond what is recorded here, hygiene layer clean (battery `SIZE=71` = 71 rows = golden TSV; all seven shared headers aggregated in `stub.cxx`; no duplicated constant literals; `TestMCNuX` still an empty compiling shell). The spec corpus (README + 12 coded specs) remains complete and coverage-closed — no spec needs authoring; the only spec defects are in-place text fixes (T28). No packet exists at runtime yet: nothing constructs a `PacketContainer`, no opacity parameters exist, no transport physics is scheduled. Everything below is implementation + test artifacts against existing specs, in priority order — the transport chain (T12 → T15 → T14 → T18) is the critical path.

## Standing facts every item inherits

- Acceptance is entirely programmatic: exact/bitwise (RNG KATs, enumerations, α=1 parity), machine ~1e-14, golden 1e-12 (Cactus harness ABSTOL=RELTOL=1e-12), relaxed 1e-10, 4σ statistical at `N_p=1048576` seed `1296518744` (secondary `20260730`), conservation 1e-13/step.
- Shared utilities are implemented ONCE and reused — never re-author: constants/units/species = `MCNuX/src/mcnux_units.hxx` (`degeneracy`, `lepton_number`, `rtol_machine`/`rtol_pinned`, `detail::approx_eq`); Philox `u(S,q,e,k)` = `MCNuX/src/mcnux_rng.hxx`; tetrad/null closure = `MCNuX/src/mcnux_tetrad.hxx` (`spatial_metric_inverse`, `p_t_closure` recomputed per call, `build_tetrad` with Gram–Schmidt pinned x→y→z, `fluid_frame_direction(u1,u2)`, `fluid_momentum_to_coordinate` — uniforms in / momenta out, NO RNG calls or `(e,k)` map inside: emission k=4/5, scattering k=2/3, diffusion k=1/2 belong to callers; callers may cache the returned PODs); TRP relabeling = `MCNuX/src/mcnux_trp.hxx` (`relabel(eta,kappa_a,kappa_s,alpha)`, 2021 α convention, α = retained fraction); ledger arithmetic = `MCNuX/src/mcnux_srcterms.hxx` (`emission/absorption/scattering_delta`, `source_rate`, `source_from_delta`; g=4 never enters ledger arithmetic); WLI call boundary = `MCNuX/src/mcnux_opacity.hxx` (`eos_evaluate`, `eos_invert_*`, `emab_evaluate`/`iso_offset`/`iso_evaluate`, `nes_pair_evaluate`/`nes_detailed_balance_fill`/`pair_crossing_fill`, `brem_evaluate_summed`, `AxisBounds` endpoint readers; species slots νe=0/ν̄e=1, νx none); packet container = `MCNuX/src/mcnux_particles.hxx` (`PacketContainer : amrex::ParticleContainerPureSoA<7,2>`, positions in real slots 0..2, `PIdx` px..w = 3..6, `IntIdx` species/event_counter = 0..1, id via `idcpu`); 4σ z-score/estimator machinery (T26, built immediately before/with T14). Any new physical constant (`hc = 1.239841984e-10 MeV cm`, `specs/opacity-eos-evaluation.md:356-361` — landed at T12 as `h_cgs`/`hc_MeV_cm`/`pinned::hc_MeV_cm`) lands in `mcnux_units.hxx` exactly once; coefficient interface = `MCNuX/src/mcnux_coefficients.hxx` (`Coefficients`, `FluidState`, `AnalyticOpacityParams`, `CoefficientSource`, `evaluate_coefficients` dispatch; parameter glue in `mcnux_coefficients.cxx`).
- Runtime-wiring fact: no code constructs a `PacketContainer` at runtime (grep `PacketContainer|GetParGDB|Redistribute|idcpu` hits only `mcnux_particles.hxx`). Runtime construction via `ghext->patchdata.at(p).amrcore->GetParGDB()` is a single implementation owned by whichever of T15/T14 lands first (T15 in this ordering); T14/T18/T22b/T23 reuse it.
- Selftest battery protocol: rows append at the END of `MCNuX/src/mcnux_selftest.cxx`, bump `SIZE=` in `MCNuX/interface.ccl:38` (currently 79; next-free row index 79), regenerate the golden TSV from a sandbox harness run — never hand-edit; routine hard-errors on table/SIZE mismatch; sequential tasks rebase on the then-current `SIZE`. Header convention (eight shared headers as of T12): flat `MCNuX/src/mcnux_*.hxx`, `namespace MCNuX`, compile-time selftests aggregate through one `#include` in `stub.cxx` (AMReX precision-gate legs live in `mcnux_particles.hxx`; `stub.cxx` keeps the double/CCTK_REAL legs). Battery rows should prefer booleans/exact values so the golden stays bitwise-stable across libm ulp differences.
- Parfile facts every parfile inherits: `Cactus::presync_mode = "mixed-error"` (required to survive CarpetX's own paramcheck), `IO::out_fileinfo = "axis labels"`, `IO::out_dir = $parfile`, `IO::parfile_write = no`, `CarpetX::out_metadata = no`; `ActiveThorns = "CarpetX IOUtil MCNuX"` suffices for Minkowski; `CarpetX::restrict_during_sync = no` whenever subcycling is on; any parfile activating `TestMCNuX` must live under `MCNuX/test/*.par` ([MCNX-BLD-06]). Test-only behaviour is gated by `STEERABLE=NEVER` booleans following the `test_synthetic_deposit` pattern (`MCNuX/param.ccl:18-29`).
- Binding anti-patterns: never use AMReX's stateful RNG (`amrex/Src/Base/AMReX_Random.H`, [MCNX-RNG-05]); never adopt CarpetX's AoS/host-loop interpolator (`CarpetX/CarpetX/src/interp.hxx:38`, [MCNX-GPU-07]/[MCNX-CTX-07]). Grid access via the `ghext` relative-include idiom proven in `mcnux_cadence.cxx` (`#include "../../../CarpetX/CarpetX/src/driver.hxx"`); ccc grid functions written via `loop_all_device<1,1,1>` from `loop_device.hxx` (`mcnux_srcterms.cxx`); `REQUIRES CarpetX` alone suffices — never add `REQUIRES AMReX`. Contributor idiom for source terms: schedule IN the empty group `MCNuX_AddToSourceTerms` with a self-accumulating READS+WRITES(everywhere) pair, `+=` only.
- Grid tallies: unordered atomic accumulation, no bitwise cross-rank/device assertions ([MCNX-RNG-08]) — review-check on every deposition task.
- The Cactus regression harness (flesh) resolves only inside the agent sandbox (`agent_scripts/sandbox/setup.sh`); harness-dependent tests run there, not in the analysis environment. Non-harness gates follow the `agent_scripts/gate_paramcheck.sh` two-leg exact-criterion pattern (transient output under `$CACTUSX/GATE/mcnux/`, never in the repo) and get CI wiring per the existing `ci.yml:163-164` precedent; `MCNXTHORNLIST` (`agent_scripts/build.sh:15,18`) is the hook for variant ThornLists.
- Consistency sweep (`specs/conventions-and-units.md:132`) forbids duplicated constant literals — standing review gate; currently clean (`mcnux_units.hxx` is the sole physical-constant literal site (incl. `h_cgs`/`pinned::hc_MeV_cm` since T12); `trpfix_*`/`srcfix_*`/`detail::pi` are fixtures/math, not violations).

## Tier 1 — Transport-chain enablers (all unblocked)

- [x] T12: Analytic/gray opacity mode + source-agnostic dispatch — per-species `kappa_a0`/`kappa_s0`/`eta_scale` parameters in `MCNuX/param.ccl` + Kirchhoff-form analytic coefficients; runtime-parameter-selected coefficient interface (table vs analytic) that T10 later plugs into; formulas pinned at `specs/verification-suite-design.md:202-233` ([MCNX-VER-05])
  - spec: specs/verification-suite-design.md
  - tests: analytic-mode coefficient values vs closed forms (machine tier); dispatch selection exactness (parameter flips source, values change accordingly).
  - notes: DONE 2026-08-23. Landed `MCNuX/src/mcnux_coefficients.hxx` (cctk-free; `Coefficients{kappa_a,kappa_s,eta}` in microphysics units, `FluidState{rho_cgs,T_MeV,Ye}`, `AnalyticOpacityParams{kappa_a0[3],kappa_s0[3],eta_scale[3]}` by `species_index`, `enum class CoefficientSource{Table,Analytic}`, `kirchhoff_eta_over_kappa_a(E,T)`, `analytic_coefficients(p,s,E,T)`, dispatch `evaluate_coefficients(src, ap, table, s, E_MeV, st) -> Coefficients` with table-callable contract `(Species, double E_MeV, const FluidState&) -> Coefficients`) + `mcnux_coefficients.cxx` glue (`selected_coefficient_source()` via first `CCTK_EQUALS` in the thorn, `analytic_params_from_parameters()`). Multi-task contract — T10 fills the table callable, T18a/T22b consume only through `evaluate_coefficients`; never rename. `param.ccl`: `KEYWORD opacity_source {table|analytic}` default `table`, `CCTK_REAL kappa_a0[3]`/`kappa_s0[3]` (default 0) / `eta_scale[3]` (default 1), range `0:*`, STEERABLE=NEVER; C++ reads `kappa_a0[i]`, parfile `MCNuX::kappa_a0[0] = ...`. `hc` now lives in `mcnux_units.hxx` (`h_cgs = 6.62607015e-27`, derived `hc_MeV_cm`, `pinned::hc_MeV_cm = 1.239841984e-10`) — T10 reuses, never restates. Battery rows 71–78 (`units.hc`, `coef.analytic.*`, `coef.dispatch.*`), `SIZE=79`, `unit-selftest.par` sets `opacity_source = "analytic"` + binary-fraction arrays; golden regenerated. Stale `.cxx.d` files naming a deleted *header* also break the build (hit once).
- [ ] T15: Geodesic push: gather + RHS + integrator + `minkowski-freestream` — trilinear-minimum interpolation of vertex-centered ADMBaseX `α, β^i, γ_ij` + order-≥2 derivatives as trivially-copyable device functor over `PacketTileData`; RHS `dx^i/dt = γ^{ij}p_j/p^t − β^i`, `dp_i/dt = −α(∂_iα)p^t + (∂_iβ^k)p_k − ½(∂_iγ^{jk})p_jp_k/p^t`; integrator free but ≥2nd order ([MCNX-GEO-01/03/04]); also owns the first runtime `PacketContainer` construction plus a deterministic synthetic packet-seeding fixture (analogue of `MCNuX_SyntheticDeposit`, `mcnux_srcterms.cxx:82-140`) and the push routine scheduled inside `MCNuX_TransportStep`
  - spec: specs/geodesic-propagation.md
  - tests: linear-field gather exactness (machine tier, synthetic `Array4`s); flat-spacetime free-stream exactness (machine tier); `minkowski-freestream` benchmark (needs no TestMCNuX code).
  - notes: centering confirmed `CarpetX/ADMBaseX/interface.ccl:12,16,20`; gather exemplar `warpx/Source/Particles/Gather/FieldGather.H:348`; uses T13 null closure at every RHS; do NOT template on CarpetX's interpolator ([MCNX-GPU-07]); spec anchor `:73-115`. Same gathered snapshot for every RHS term. Needs T8 + T13; first cross-thorn READS resolves the INHERITS-vs-bare-READS open question empirically. This plan run: T8/T13 are done, so T15 is unblocked; call `spatial_metric_inverse`/`p_t_closure` from `mcnux_tetrad.hxx:93-104,118-122` — never reimplement; `PacketTileData` is at `mcnux_particles.hxx:93`; the warpx gather is a structural pattern only; record the INHERITS-vs-READS answer in the Decisions section; `minkowski-freestream` = `specs/verification-suite-design.md:241`, golden 1e-12 on Minkowski; spec `:198-203` defers cadence to CTX — the T4 cadence group is the trigger point. Unblocks T16, T18a (cell-exit time), T22b, T23.
- [ ] T6: TestMCNuX Schwarzschild initial data — `SHARES: ADMBaseX` + `EXTENDS KEYWORD initial_data`/`initial_lapse` adding `"Schwarzschild"`; writers `IN ADMBaseX_InitialData`/`ADMBaseX_InitialGauge` with full `WRITES: (everywhere)`; isotropic `ψ=1+M/(2r)`, `γ_ij=ψ⁴δ_ij`, `K_ij=0`, `α=(1−M/(2r))/(1+M/(2r))`, `β^i=0` ([MCNX-VER-02/03])
  - spec: specs/verification-suite-design.md
  - tests: smoke parfile + golden TSV comparing grid output against the closed forms (machine/golden tier).
  - notes: fully unblocked — depends only on stable base thorns; anchors `specs/verification-suite-design.md:139-181`, `CarpetX/ADMBaseX/schedule.ccl:12,16,68-104`, `CarpetX/ADMBaseX/interface.ccl:12-20`; shift needs no writer (default `"zero"`). First `.cxx` under `TestMCNuX/src/` — update its `SRCS`. Risk (spec `:456-461`): cross-repository `EXTENDS KEYWORD` mechanism unregressed in flesh — this task confirms it empirically. Re-verified 2026-08-23 (`ADMBaseX/schedule.ccl:68-107`): no thorn in the checked-out CarpetX stack uses cross-repo `EXTENDS KEYWORD`, so this task IS the test; if it fails, the spec's documented fallback (own keyword + `"none"` base values, `:458-466`) requires a spec-author amendment before proceeding. Unblocks T16.
- [ ] T7: TestMCNuX hydro profiles — `SHARES: HydroBaseX` + `EXTENDS KEYWORD initial_hydro` adding `"uniform sphere"`, `"equilibration box"`; writers `IN HydroBaseX_InitialData` for `(ρ,T,Yₑ)` profiles, `vel=0`, other fields zeroed, temperature in MeV; parameters for `M`, sphere center/radius, `(ρ₀,T₀,Yₑ₀)`, ambient values ([MCNX-VER-04])
  - spec: specs/verification-suite-design.md
  - tests: smoke parfile + golden comparing profile fields to formulas (golden tier); grid pinned so sharp-surface cells are ≤1% volume effect (spec `:456-493` assumption 4).
  - notes: independent build+test cycle from T6 (separate keyword family/writer); anchors `specs/verification-suite-design.md:182-201`, `CarpetX/HydroBaseX/interface.ccl:11-29`, `CarpetX/HydroBaseX/schedule.ccl:17,59-75`. Needed by `stats-*`, `interactions-fixedseed`, T29. Re-verified 2026-08-23: `initial_hydro` is keyword-conditional with only `"vacuum"` today (`schedule.ccl:59-60`); `HydroBaseX/interface.ccl:19` carries no unit annotation on temperature — MeV is MCNuX's contract, state it in the writer.
- [ ] T10: Baseline coefficient assembly + νx mapping + `hc` constant — `κ_a(s,E)=EmAb`, `κ_s(s,E)=Iso(iMom=0)`, `η(s,E)=c·κ_a·f_eq·4πE³/(hc)³` for νe/ν̄e from EmAb+Iso with Kirchhoff closure using EOS chemical potentials at (ρ,T,Yₑ); νx: `κ_a=0`, `η=0`, `κ_s` = ½-mean of the two electron-type Iso values ([MCNX-OPA-04/05]); plugs into T12's dispatch interface
  - spec: specs/opacity-eos-evaluation.md
  - tests: Kirchhoff-closure identity checks at pinned table points; νx zero/half-mean identities (machine tier); selftest rows.
  - notes: depends on T9; NES/Pair/Brem thermal-emission assembly is explicitly out of scope — do not attempt (spec `:340-349`). If the `hc` constant is needed it goes in `mcnux_units.hxx` once. This plan run: T9 done → unblocked; use `emab_evaluate`/`iso_evaluate`/`eos_evaluate` (`mcnux_opacity.hxx:279-309`); species slots exist (`mcnux_opacity.hxx:233-268`) but the νx mapping arithmetic does not; spec anchor `:193-239`; pinned-table-point checks use synthetic in-memory tables as in T9 (no HDF5 in CI). Needed by T29.
- [ ] T11: Table-range enforcement — transparency floor (ρ<ρ_min ⇒ all coefficients zero, no table touch), component-wise clamping elsewhere, per-axis clamp diagnostic counters, no-NaN guarantee ([MCNX-OPA-06]); `EosInversionResult{T,Error}` hard-fail-in-test-mode protocol ([MCNX-OPA-07]) may be stubbed since baseline transport receives T in MeV externally
  - spec: specs/opacity-eos-evaluation.md
  - tests: range-policy selftests — out-of-range probes return clamped/zero values with correct counter increments, never NaN (exact); inversion-protocol fixture.
  - notes: WeakLibInterp extrapolates permissively — does zero range-checking by design (`wli_eos.H:50-52`) — so enforcement is entirely MCNuX's (README boundary policy); spec anchor `:174-272`. Inversion leg stub-eligible: baseline path never inverts (spec `:261-263`). This plan run: build on `AxisBounds`/`axis_bounds`/`axis_bounds_temperature_mev` (`mcnux_opacity.hxx:410-437`, explicitly "not OPA-06") and `EosInversionResultMeV{T_MeV,error}` (`mcnux_opacity.hxx:145-156`); spec anchor narrowed to `:241-263`.
- [ ] T26: Statistical-acceptance shared machinery — shared 4σ estimator/z-score reduction (`estimate`, `expected`, `σ`, `z=(estimate−expected)/σ`) used by `stats-emission`/`stats-beam`/`stats-scatterbox`/`stats-diffusion`/`stats-equilibration`/`trp-overlap-consistency` at `N_p=2^20`, seeds `1296518744`/`20260730` ([MCNX-RNG-07], [MCNX-VER-07])
  - spec: specs/rng-and-statistical-acceptance.md
  - tests: exercised by every stats benchmark; contract at `specs/verification-suite-design.md:255-279`.
  - notes: build WITH the first stats benchmark (T14) and reuse — do not duplicate per benchmark; listed separately so consolidation is explicit. Per-check estimate/expected/σ/z written as grid scalars/norm outputs; σ formula comes from each consuming benchmark's leaf spec; API shape free (`specs/rng-and-statistical-acceptance.md:125-130`). This plan run: grep-confirmed zero prior art in `MCNuX/src`; the pure reduction is unblocked now — add fixture selftest rows for it (exact/machine) immediately before T14 so `stats-emission` consumes it end-to-end.

## Tier 2 — Transport core

- [ ] T14: Emission sampling: count law + packet creation + `emission-fixedseed`/`stats-emission` — packet state schema onto T8 container ([MCNX-PKT-01]); emission count `N_p = g_s √−g ΔV Δt η_b(s)/E_p` with floor/Bernoulli remainder, g=4 applied exactly once at creation via `MCNuX::degeneracy` ([MCNX-PKT-02]); bin-center energy, weight `N=E_p/ν`, uniform position/time draws ([MCNX-PKT-03]); pinned `(q,e,k)` creation draw map (cell-level Bernoulli at `(q=K_cell, e=n, k=3b+s)`, then k=0..5 per packet) with `K_cell` bit packing ([MCNX-PKT-05]); `E_p` parameter in `param.ccl`; id uniqueness via `idcpu`, bit 63 never set ([MCNX-GPU-04] creation leg)
  - spec: specs/packet-representation-and-sampling.md
  - tests: `emission-fixedseed` benchmark — exact per-packet event/state sequence at pinned seed, single rank, golden parity 1e-12; `stats-emission` — count/energy/species estimators within 4σ at `N_p=2^20`, seed `1296518744`, archived z-values then golden-frozen at 1e-12 (no seed-shopping on a false 4σ failure); draw-map audit trace (exact); id-contract test (bit 63, uniqueness).
  - notes: uses T12 analytic η; direction draw consumes T13; anchors spec `:90-162`, `specs/particle-container-and-gpu.md:180-188`. Builds T26's machinery with it. This plan run: spec anchor widened to `:90-170` ([MCNX-PKT-06] included); `degeneracy` at `mcnux_units.hxx:180,188-189`; reuses T15's runtime container construction (or owns it if sequenced first); the coverage matrix (`specs/verification-suite-design.md:399`) homes [MCNX-GPU-04]'s bullet in `interactions-fixedseed` — T14 carries the creation leg, T18b closes it with retirement; the cell-key capacity assumption (patch<128, level<256, extent<65536, spec `:230-248`) has no guard — add a cheap runtime assert; packet-count control beyond `E_p` stays spec-deferred (`:245-248`).
- [ ] T16: `schwarzschild-pt` benchmark — parfile + golden data: packet orbits on T6's Schwarzschild background, `p_t = −α²p^t + β^ip_i` tracked along the trajectory, drift ≤1e-5 at `h=M/16`, `T=100M` ([MCNX-GEO-05])
  - spec: specs/geodesic-propagation.md
  - tests: the benchmark itself (explicit numeric tolerance pinned per benchmark) + convergence order ≥2 across Δt, Δt/2, Δt/4 + golden parity 1e-12.
  - notes: depends on T6 + T15; separate build+test cycle from both; anchors spec `:117-132`, `specs/verification-suite-design.md:241-242`. The 1e-5 drift bound is a design bound, re-pinnable at golden-creation time per spec `:456-493`. This plan run: nothing computes `p_t` along a trajectory today (only the null-closure form exists); this is the first parfile activating `TestMCNuX` — it must live under `MCNuX/test/*.par` ([MCNX-BLD-06]).
- [ ] T18a: Interaction draw + episode competition + RNG map — `Δt_{s,a} = −ln(1−u_{s,a}) p^t/(κ_{s,a} ν)`; per-cell episode boundaries (creation/step-start, post-scatter, cell-crossing — normative deviation from papers, golden-determining); shorter-of firing vs cell exit and Δt_rem; zero-opacity `+∞` sentinel, no NaN; tie-break to scattering; pinned `(k=0..3)` draw order, undrawn-remainder discard, e incremented per episode ([MCNX-INT-01/02/06])
  - spec: specs/neutrino-matter-interactions.md
  - tests: draw-map audit trace (exact); unit tests with synthetic `p^t`/cell-exit inputs; NaN-free zero-opacity fixture; `interactions-fixedseed` golden parity; `stats-beam` via T26 machinery.
  - notes: spec anchors `:83-126,165-179`; coefficients exclusively via T12/T10 interface, cell-centered ([MCNX-INT-05]) — never re-derived. Preserve spec-normative deviations verbatim (per-cell episode redraw, frozen episode kinematics, tie→scattering, spec `:249-271`). This plan run — dependency previously unlisted: the cell-exit-time source is T15's pusher; build the competition logic as pure functions against synthetic inputs first (testable via battery rows before integration), integrate once T15 has landed; `interactions-fixedseed` also needs T7 hydro data; draw slots `k=0(u_s),1(u_a),2,3`.
- [ ] T18b: Absorption — full packet removal (no weight decay), full-content transfer to T17 ledger via `absorption_delta`, id retirement (never reused) ([MCNX-INT-03]; [MCNX-GPU-04] removal leg)
  - spec: specs/neutrino-matter-interactions.md
  - tests: `stats-beam` — beam attenuation e^{−κΔ} within 4σ; discreteness check (weights never decay); ledger transfer sums exact per event; [MCNX-GPU-04] removal leg in `interactions-fixedseed` — retired ids never reappear (closes the coverage-matrix bullet at `specs/verification-suite-design.md:399`).
  - notes: part of the `interactions-fixedseed` golden benchmark (`specs/verification-suite-design.md:244`, needs T7 hydro data). This plan run: ledger arithmetic exists (`mcnux_srcterms.hxx:72-78`), the event side does not; spec anchor `:128-135`; needs T18a.
- [ ] T18c: Elastic scattering — isotropic fluid-frame redraw at fixed ν via T13 tetrad (`fluid_frame_direction`/`fluid_momentum_to_coordinate`, k=2/3 drawn by the caller); momentum-change into ledger via `scattering_delta` ([MCNX-INT-04])
  - spec: specs/neutrino-matter-interactions.md
  - tests: `stats-scatterbox` — isotropy + elasticity (ν preserved, machine tier) within 4σ; fixed-seed event-sequence parity.
  - notes: reuse T13's tetrad — never reimplement (spec `:136-154`). This plan run: primitives exist (`mcnux_tetrad.hxx:239,290`, `mcnux_srcterms.hxx:79-84`), event missing; per-episode tetrad caching is spec-permitted (`mcnux_tetrad.hxx:178`); needs T18a.
- [ ] T19: Conservation-ledger closure check — per-step audit: event-side sums `Σ_events δX` vs grid totals `Σ_cells(source)·ΔV·Δt` for energy/momentum/lepton number (all five channels), relative 1e-13/step with an `ε₀` floor for the all-zero case; test-configuration-only allowed; escape tallies (per-species energy/number at outer boundary, README boundary policy) belong to this ledger surface ([MCNX-HYD-05])
  - spec: specs/hydro-coupling-source-terms.md
  - tests: closure to 1e-13/step in fixed-seed benchmarks (T14/T18 configurations), asserted as a golden pass flag.
  - notes: depends on T17 + T18; spec anchors `:177-188,295-296`. Synthetic-event arithmetic validation can land with/after T17; real closure needs real producers. Always-on vs test-only audit computation is an implementer decision (spec `:295-296`). This plan run: grep-confirmed no audit/escape code exists; the implementer must also record the escape-tally home (new MCNuX-owned grid variables vs audit-internal bookkeeping) in the Decisions section.
- [ ] T20: `cadence-lag` benchmark — golden benchmark demonstrating source terms advance exactly once per coarsest Δt with the one-step lag semantics ([MCNX-CTX-05] verification): step-function fluid perturbation at iteration n first affects source terms at n+1
  - spec: specs/verification-suite-design.md
  - tests: golden parity 1e-12 (counter + source-term tally checksum TSV).
  - notes: depends on T4 + T14/T17; spec anchor `:238-247`. Counter half already exists (`transport_step_count` in `MCNuX/interface.ccl`).
- [ ] T23: GPU redistribution + ownership invariants — local `Redistribute`/`RedistributeLocal(N)` inside `MCNuX_TransportStep` after the last position-changing op (and after regrid), gated on bounded motion (N from a proven per-step motion bound, else global `Redistribute()`); after-step ownership invariant: every packet on the rank/level/grid/tile owning its position; escapees removed/tallied, never redistributed ([MCNX-GPU-05/06])
  - spec: specs/particle-container-and-gpu.md
  - tests: `ownership-multibox` harness benchmark (≥2 boxes, ownership after step, golden); bounded-motion audit; single-rank determinism check (bitwise-identical populations across two fixed-seed runs — rides on T14's fixedseed benchmark).
  - notes: depends on T4 step loop + T15 motion; spec anchor `:190-209`; `specs/verification-suite-design.md:245`. This plan run: Minkowski, ≥2 boxes per rank via small `CarpetX::max_grid_size` under `NPROCS 1`, packets crossing box boundaries; reuse T27b's multi-box parfile convention (may absorb T27b's counter golden if sequenced together); the "synthetic motion kernel to unblock earlier" option is not taken — T15 supplies the real bound; redistribution under subcycling (spec `:308-311`) stays open and moot while `use_subcycling = no`.

## Tier 3 — Trapped regime & equilibration

- [ ] T21b: α selection + TRP parameter surface + diagnostic — pure `alpha_select(kappa_a, dt_c, xi) → min(1, ξ/(κ_a Δt_c))`, fixed-α override parameter, `κ_a′Δt_c ≤ ξ` per-cell diagnostic; `MCNuX/param.ccl` entries for ξ (default 1), τ_diff (default 3), scheme selector explicit/relabeled (names free, semantics binding, spec `:434-436` — param.ccl currently has zero TRP entries) ([MCNX-TRP-04])
  - spec: specs/trapped-regime-treatment.md
  - tests: selection-rule selftest rows (exact) incl. α=1 branch and override precedence; golden regen.
  - notes: bitwise-limit benchmark needs T18; α-selection anchor `:145-171`. This plan run: SPLIT — the `trp-alpha-limit-*` benchmarks moved to T21c so the unblocked parameter/selection half can land now; consumes `MCNuX::relabel()` — never re-author; the implementer records whether the diagnostic is a battery check or a runtime grid scalar (spec `:169,370-372`, freedom `:421-439` — affects whether `interface.ccl` gains a variable).
- [ ] T22a: TRP diffusion kinematics pure functions (standalone math half) — `p_ns = exp(−κ_s′Δt′_rem)`, truncated-Gaussian displacement branch selector (`P < 1−p_ns`) + closed-form antiderivative `F` inversion (≤1e-12 in r̃), causal cap `r_d ≤ Δt′_rem` ([MCNX-TRP-08]); correlated momentum θ₂ draw with 21-point `B_i` table (`1000, 18.74, 7.52, …`) + linear interpolation, `cosθ₂ = B − (1+B)((B−1)/(B+1))^r`, atom-branch `cosθ₂=1` at `f_free=1` ([MCNX-TRP-10])
  - spec: specs/trapped-regime-treatment.md
  - tests: selftest rows — F/F⁻¹ round-trip ≤1e-12; branch-selector fixtures; `B_i` interpolation fixtures; atom-branch pin (exact).
  - notes: spec anchors `:219-321`. Split this plan run: pure scalar math is unblocked and testable now via battery rows; the integration leg (two-leg position update, [MCNX-TRP-09]) moved to T22b with the rest of the blocked dispatch work. Re-confirmed 2026-08-23: grep-confirmed absent; anchors narrowed to `:219-247,279-321`.
- [ ] T21c: `trp-alpha-limit-{explicit,relabeled}` bitwise-limit benchmarks — α=1 relabeled run reproduces the explicit run BITWISE (events, population, golden) at fixed seed, single rank ([MCNX-TRP-05]; [MCNX-TRP-01] explicit baseline is verified here — no standalone artifact by design)
  - spec: specs/trapped-regime-treatment.md
  - tests: α=1 run reproduces explicit run BITWISE at fixed seed, single rank (spec `:173-181`; `specs/verification-suite-design.md:247,375`), golden 1e-12.
  - notes: bitwise-limit benchmark needs T18; α-selection anchor `:145-171`. SPLIT from T21b this plan run; needs T14, T18, T21b; record explicitly that [MCNX-TRP-01] is folded into this pair.
- [ ] T22b: TRP regime entry + episode dispatch + position/momentum integration + 5-draw map — `κ_s′Δt′_rem > τ_diff` evaluated after EVERY performed scattering (documented deviation, spec `:182-217,507-522`), one diffusion episode per (packet, step), no absorption channel inside; two-leg position update (advection leg `x^i ← x^i + (u^i/u^t)(1−f_free)Δt_rem` + free-streaming leg via T15's pusher, [MCNX-TRP-09]); tetrad-transformed momentum outcome via T13; fixed five-draw order `k=0..4` (P, cosθ_f, φ_f, r, φ₂) ([MCNX-TRP-06/07])
  - spec: specs/trapped-regime-treatment.md
  - tests: `stats-diffusion` (three-clause, 4σ via T26); `trp-overlap-consistency`; scheme identities; diffusion-leg draw-map audit in `interactions-fixedseed` (spec `:361-406`).
  - notes: depends on T18. Also needs T13, T15, T22a. Preserve the spec's four logged deviations verbatim (`:443-557`). This plan run: also needs T21b (scheme selector, τ_diff); anchors `:182-217,249-277,323-339`; Eq. 50 β-dependent bound remains spec-deferred (`:450-455`).
- [ ] T29: `stats-equilibration-{explicit,relabeled}` benchmarks — equilibration-box configuration driving the full emission/absorption/scattering chain to thermal equilibrium; arbiter of OPA's stimulated-absorption and chemical-potential conventions (`specs/opacity-eos-evaluation.md:338-379`) ([MCNX-VER-07])
  - spec: specs/verification-suite-design.md
  - tests: 4σ-then-golden protocol via T26 machinery at `N_p=2^20`, seed `1296518744`; each variant documents its own σ formula from its governing leaf spec (`:255-279`).
  - notes: new this plan run — no prior TODO owner. Needs T7 (equilibration box), T10 (table coefficients), the T14/T18 chain, and T21a/T21b for the relabeled variant. Reuse T26 — no new statistical machinery. Carried forward 2026-08-23 unchanged; relabeled variant also needs T21c's bitwise-limit pair as its baseline.

## Tier 4 — Optional physics & GPU delivery

- [ ] T24: Optional Tmunu contribution — `REQUIRES TmunuBaseX` in `configuration.ccl`; parameter-gated (BOOLEAN, default off, gates storage + scheduling) routine `IN TmunuBaseX_AddToTmunu` (`CarpetX/TmunuBaseX/schedule.ccl:31-33`), MC stress-energy estimator (arXiv:1708.08452v2 Eq. 16 as restated in spec) with exact 8-point cell-to-vertex average `eT(v) += Σ_{d∈{0,1}³} q(v−d)/8`; writes only through TmunuBaseX's extension group ([MCNX-HYD-06/07])
  - spec: specs/hydro-coupling-source-terms.md
  - tests: deposition-identity unit test for the 8-point average (machine tier) + sign fixtures; gated-off default leaves eT* untouched (exact).
  - notes: deferrable; unresolved double-counting question when enabled with four-force sources on evolving spacetime — spec amendment required before that combined path; do not enable both in any committed parfile (spec `:190-222,330-336`). Never mark this and the four-force coupling simultaneously "done" without addressing the open question. This plan run: needs a packet population (T14); no cell-to-vertex code exists anywhere yet.
- [ ] T25d: GPU-backend build of the pinned ThornList + device RNG purity leg ([MCNX-BLD-03/04], [MCNX-GPU-02], [MCNX-RNG-06] device leg)
  - spec: specs/build-and-integration.md
  - tests: GPU-backend configure+build green; precision gate static_asserts hold with AMReX headers; per-packet-operator device-kernel inspection; device-vs-host bitwise equality of `MCNuX::u` over the `mcnux_rng.hxx` `detail::purity_batch` tuples (exact).
  - notes: currently only a CPU/MPI cfg exists in the sandbox. CI compile-only cuda/rocm matrix already exists (`.github/workflows/ci.yml:173-247`); missing legs are the sandbox GPU harness run and the deferred host-vs-device RNG-purity check (deferral recorded in `mcnux_rng.hxx:32-40`; discovered 2026-08-14 during T2 — the CPU/MPI sandbox cannot exercise this leg, so T2 shipped with all host-side purity legs asserted at compile time). GPU-vs-golden norm tolerances explicitly undecided pending first GPU run (spec-recorded open). This plan run: `agent_scripts/sandbox/setup.sh` builds AMReX CPU-only via mpicc/mpicxx with no `AMReX_GPU_BACKEND` — a GPU AMReX build + second `mcnux` configuration is part of this task; `purity_batch` at `mcnux_rng.hxx:382-457`; the KAT must hold host+device (`specs/verification-suite-design.md:346`); CI matrix self-describes as not correctness-bearing (`ci.yml:171`).

## Tier 5 — Gates & spec hygiene (unblocked, low urgency)

- [ ] T27a: Validity-machinery gate — gate script running an existing-machinery parfile (`source-zero-add.par` or `unit-selftest.par`) with `CarpetX::poison_undefined_values = yes` (clean pass) plus a deliberately under-declared READS/WRITES meta-test variant (test-only param-gated routine) asserting the driver's checksum/poison error fires (`specs/carpetx-thorn-integration.md:204-208`, [MCNX-CTX-04] verification)
  - spec: specs/carpetx-thorn-integration.md
  - tests: the gate itself is the test — exact-criterion pass/fail on both legs.
  - notes: currently satisfied by nothing — no parfile sets `poison_undefined_values`, no meta-test exists; needs no new physics. Precedent `agent_scripts/gate_paramcheck.sh`; planner decision taken: gate-now (spec treats the bullet as independent), CI wiring per precedent. This plan run: the implementer records whether the "(and a presync-mode test run)" clause (`:205`) is already met by the standing `presync_mode = "mixed-error"` convention or needs an explicit leg.
- [ ] T27b: Scheduling-mode multi-box artifact — committed reproducible artifact (harness benchmark parfile with ≥2 boxes per rank via small `CarpetX::max_grid_size` + golden `transport_step_count` output, or a gate script asserting the invocation count) proving exactly one observer invocation per (patch,level) traversal (`specs/carpetx-thorn-integration.md:209-211`, [MCNX-CTX-03] verification)
  - spec: specs/carpetx-thorn-integration.md
  - tests: golden `transport_step_count` TSV (or gate exact-count check).
  - notes: verified only manually during T4 (64-box rerun, 10 firings in 10 iterations) — not reproducible via `agent_scripts/test.sh` today. May later merge with T23's `ownership-multibox`, but needs no particle code and can close now. This plan run: kept standalone; T23 reuses its multi-box parfile convention; matrix row `specs/verification-suite-design.md:403-409`.
- [ ] T25b: Configure-time negative-path gate — script building a `mcnux.th`-derived ThornList variant omitting `CarpetX/CarpetX` and/or `WeakLibInterp/WeakLibInterp` (reuse `MCNXTHORNLIST`) and asserting configure-time failure distinct from other failure modes ([MCNX-BLD-02] verification)
  - spec: specs/build-and-integration.md
  - tests: gate script's own pass/fail (exact); script location convention is implementation freedom.
  - notes: spec anchor `:186-189`; CI wiring per gate precedent. This plan run: variant lists committed under `agent_scripts/gates/` or generated in-script (implementer's choice, spec `:222-224`).
- [ ] T25c: Activation-split audit sweep + layout audit + RNG-05 grep gate — script asserting `TestMCNuX` appears in ActiveThorns only within `MCNuX/test/*.par` ([MCNX-BLD-06]) + smoke configure/build/start with a ThornList omitting `MCNuX/TestMCNuX`; fold in the layout-audit bullet (`specs/build-and-integration.md:203-206` — exactly two thorns, no `TestMCNuX/test/`, 13 spec files + validator) and a grep gate failing on `AMReX_Random.H`/`AMReX_RandomEngine.H`/`ParallelForRNG` anywhere in `MCNuX/src/` ([MCNX-RNG-05] made mechanical)
  - spec: specs/build-and-integration.md
  - tests: sweep script pass/fail (exact).
  - notes: relevant once parfiles exist (after T5/T14). Parfiles now exist (`unit-selftest.par`); property currently holds by inspection. Planner decision taken: layout audit folds into this script rather than staying manual. This plan run: four parfiles exist, none activates `TestMCNuX`; the no-`TestMCNuX` smoke build has never run (pinned `mcnux.th` always includes it — needs a second config or an `MCNXTHORNLIST` swap; implementer records the choice); CI wiring per precedent; the RNG-05 grep consolidation is new (near-zero cost, turns a review convention into a check).
- [ ] T28: Spec-corpus text fixes — (a) amend `specs/conventions-and-units.md:129`, which demands `1e-14` relative agreement against the *pinned 10-digit* factor values — arithmetically impossible (truncated literals deviate from full-precision recomputation by up to 2.1e-10; worst: emissivity 2.06e-10); T1 resolved it per [MCNX-CNV-04] (extra digits allowed) with `rtol_pinned=1e-9` vs pinned literals and `rtol_machine=1e-14` for full-precision identities — the amendment states this two-tier tolerance so the `unit-selftest` criterion is satisfiable; (b) fix the validator invocation `bash tools/validate_specs.sh` → `bash specs/tools/validate_specs.sh` corpus-wide (17 occurrences across `specs/README.md:103`, `conventions-and-units.md:136`, `geodesic-propagation.md:166`, `trapped-regime-treatment.md:410`, `carpetx-thorn-integration.md:215`, `neutrino-matter-interactions.md:227`, `opacity-eos-evaluation.md:314`, `hydro-coupling-source-terms.md:279`, `verification-suite-design.md:287,336,427`, `particle-container-and-gpu.md:268`, `build-and-integration.md:205,210`, …)
  - spec: specs/conventions-and-units.md
  - tests: `bash specs/tools/validate_specs.sh` exit 0 after the edit (exact).
  - notes: discovered 2026-08-14 during T1; low urgency — code already carries the rationale (commented in `MCNuX/src/mcnux_units.hxx`). Scope includes authoring the amendment via spec-author (no new spec file; edit in place); bundle both edits in one iteration. This plan run: (b) is wider than previously recorded — every coded spec's Verification section carries the stale invocation, not just the README; fix all in the same iteration (text-only; `validate_specs.sh:10-30` does not inspect this string); the worked derivation for (a) is at `mcnux_units.hxx:35-48`.

## Decisions & non-blocking spec items

- INHERITS vs bare READS: whether `READS:` on `ADMBaseX::`/`HydroBaseX::` groups requires `INHERITS:` in `interface.ccl` under CarpetX flesh — still open; no cross-thorn READS exists anywhere yet (`MCNuX/interface.ccl:8-9,47`); T15 (or T7's writers) determines empirically at first cross-thorn READS and records the answer here.
- Cross-repository `EXTENDS KEYWORD` viability — unconfirmed in the checked-out stack; T6 is the empirical test; failure path requires a spec-author amendment to `specs/verification-suite-design.md:139-147,458-466` (documented fallback: own keyword + `"none"` base values).
- RESOLVED at T4 — PARAMCHECK vs schedule-tree ordering: the flesh silently ignores an unresolvable `AFTER ODESolvers_Solve` (no schedule-resolution error precedes the T3 paramcheck abort; verified both with ODESolvers inactive and with `use_subcycling=yes` where only `ODESolvers_Solve_Subcycling` exists). Caveat: with the AFTER target absent the group's placement in `[CCTK_EVOL]` is arbitrary (observed *before* `ODESolvers_Solve_Subcycling`) — T3's guard is therefore load-bearing for [MCNX-CTX-05], not merely defensive; any future subcycling extension must revisit the ordering clause, not just drop the guard.
- RESOLVED at T8 — header/gate permanent home: `stub.cxx` does not retire; each shared header carries its own compile-time selftests (AMReX precision-gate legs live in `mcnux_particles.hxx`), `stub.cxx` keeps the plain double/CCTK_REAL legs and remains the aggregation point including every shared header.
- RESOLVED — `.gitignore` build products unnecessary: multiple sandbox builds (T3–T5) completed with the repo staying clean (build lives in mounted `$CACTUSX` tree per `agent_scripts/sandbox/README.md:39`).
- Gate CI wiring: CLAUDE.md calls gates "sandbox only" but `gate_paramcheck.sh` already runs in CI (`ci.yml:163-164`) — precedent decided: new gates (T27a, T27b, T25b, T25c) get CI wiring too.
- Scripted no-duplicate-literal sweep joining `validate_specs.sh` vs staying a manual review gate — tooling scope, not blocking; revisit if a violation ever slips through review. (The [MCNX-RNG-05] grep, by contrast, is folded into T25c.)
- T4 recovery caveat (not exercised): if CarpetX doesn't mark recovered scalars valid under mixed-error, schedule the init routine also `AT CCTK_POST_RECOVER_VARIABLES`.
- Implementer-recorded choices pending: T27a presync-mode clause reading (explicit leg vs satisfied by `mixed-error`); T19 escape-tally home (grid variables vs audit-internal) and always-on vs test-only audit; T21b diagnostic as battery check vs runtime grid scalar; T25c separate Cactus config vs `MCNXTHORNLIST` swap for the no-`TestMCNuX` smoke build; T25d GPU-vs-golden norm tolerances (spec-undecided until the first GPU run).
- [MCNX-GPU-04] is verified across two tasks by design: creation leg (bit 63 clear, uniqueness) in T14, removal/retirement leg in T18b, which closes the single coverage-matrix bullet (`specs/verification-suite-design.md:399`). [MCNX-TRP-01] has no standalone artifact by design — T21c's bitwise-limit pair is its verification.
- `source-zero-add` is an extra benchmark outside the pinned [MCNX-VER-06] set (permitted, `specs/verification-suite-design.md:449-450`); no matrix row required.
- Explicitly spec-deferred (NOT deliverables): packet-count control beyond `E_p` (`specs/packet-representation-and-sampling.md:245-248`); β-dependent Eq. 50 stability bound (`specs/trapped-regime-treatment.md:450-455`); NES/Pair/Brem νx thermal-emission assembly (`specs/opacity-eos-evaluation.md:340-349`); HYD-06 simultaneous-use double-counting (`specs/hydro-coupling-source-terms.md:330-336`); redistribution under subcycling (`specs/particle-container-and-gpu.md:308-311`).

## Discovered since last plan
