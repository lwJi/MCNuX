# TODO — MCNuX build plan

Foundations and all Tier-1 core enablers of the previous plan are in: T1–T5, T8, T9, T13, T17, T21a, T25a landed (units header, Philox RNG header, subcycling paramcheck guard + gate, transport cadence group, pure-SoA `PacketContainer`, WeakLibInterp call-boundary wrapper, tetrad/null-closure helpers, source-term grid variables with zero-then-add schedule + `source-zero-add` harness test, TRP relabeling map, `unit-selftest` battery with 71 rows, spec-corpus validator green; last build `.build/t21a-trp-map/build.md`: build PASS, harness 2/2 PASS). The spec corpus (README + 12 coded specs) remains complete and coverage-closed — no specs need authoring; the only spec defects are cosmetic text fixes (T28). Codebase is clean: no drift, no duplicated constant literals, `TestMCNuX` is still an empty compiling shell, `MCNuX/param.ccl` carries zero opacity/TRP/`E_p` parameters, no `INHERITS:`/cross-thorn `READS:` exists yet, `PacketContainer` is never instantiated at runtime, and `MCNuX_TransportStep` holds only the cadence observer plus the source-term zero/synthetic-deposit pair. Everything below is implementation + test artifacts against existing specs, in priority order; this cycle re-scoped T21b (split off T21c), redrew the T14/T26 boundary, and surfaced two spec-covered but previously untracked verification items (T30, T31).

## Standing facts every item inherits

- Acceptance is entirely programmatic: exact/bitwise (RNG KATs, enumerations, α=1 parity), machine ~1e-14, golden 1e-12 (Cactus harness ABSTOL=RELTOL=1e-12), relaxed 1e-10, 4σ statistical at `N_p=1048576` seed `1296518744` (secondary `20260730`), conservation 1e-13/step.
- Shared utilities are implemented ONCE and reused: constants/units/species = `MCNuX/src/mcnux_units.hxx` (done, reuse `species_lepton_number`/`degeneracy`/`Species::NuX`/`detail::approx_eq`/`rtol_machine`/`rtol_pinned` — never re-author; `hc = 1.239841984e-10 MeV cm` is NOT yet present and lands here exactly once in T10 following the pinned/derived dual-tolerance pattern at `mcnux_units.hxx:221-283`); Philox `u(S,q,e,k)` = `MCNuX/src/mcnux_rng.hxx` (done); tetrad transform in fixed (x,y,z) Gram–Schmidt order + 3×3 inverse-metric/null-closure helper = `MCNuX/src/mcnux_tetrad.hxx` (done, consumers T14, T15, T18c, T22b — never re-author); TRP relabeling map `relabel()` = `MCNuX/src/mcnux_trp.hxx` (done, T21b/T22a extend this header — never re-author); ledger deltas `emission/absorption/scattering_delta` + `source_rate`/`source_from_delta` = `MCNuX/src/mcnux_srcterms.hxx` (done, consumers T18b/c, T19, T30); WeakLibInterp wrappers `eos_evaluate`/`emab_evaluate`/`iso_evaluate`/`AxisBounds` = `MCNuX/src/mcnux_opacity.hxx` (done, T10/T11 build on it — never re-wrap WLI); T12's source-agnostic coefficient interface (consumers T10, T14, T18, T21b — T10 must NOT invent a second interface shape); T26's stats header (all `stats-*` benchmarks — never duplicate per benchmark).
- Runtime `PacketContainer` instantiation (`ghext->patchdata.at(p).amrcore->GetParGDB()`, deferral comment `mcnux_particles.hxx:82-84`) is owned by whichever of T14/T15 lands first; the other reuses it. If T15 lands first it needs a test-gated synthetic packet seeder (mirror the `test_synthetic_deposit` STEERABLE=NEVER pattern).
- Selftest battery protocol: rows append at the END of `MCNuX/src/mcnux_selftest.cxx` (rows 0..70 populated; re-read the row-map comment at `mcnux_selftest.cxx:29-63` before appending), bump `SIZE=` in `MCNuX/interface.ccl:38` (currently 71; next-free row index 71 — new opacity rows append at 71, not after the 38–52 opacity block), regenerate the golden TSV from a sandbox harness run — never hand-edit; routine hard-errors on table/SIZE mismatch. Header convention: flat `MCNuX/src/mcnux_*.hxx`, `namespace MCNuX`, compile-time selftests aggregate through one `#include` in `stub.cxx` (currently all seven headers).
- Parfile facts every parfile inherits: `Cactus::presync_mode = "mixed-error"` (required to survive CarpetX's own paramcheck), `IO::out_fileinfo = "axis labels"`, `IO::out_dir = $parfile`, `IO::parfile_write = no`, `CarpetX::out_metadata = no`; `ActiveThorns = "CarpetX IOUtil MCNuX"` suffices for Minkowski; `CarpetX::restrict_during_sync = no` whenever subcycling is on. Templates: `MCNuX/test/unit-selftest.par:17-55`, `MCNuX/test/source-zero-add.par:25-68`. All benchmarks live under `MCNuX/test/`, never `TestMCNuX/test/` ([MCNX-BLD-05]).
- Contributor idiom for real event depositors: schedule `IN MCNuX_AddToSourceTerms` (`MCNuX/schedule.ccl:114-121`) with a self-accumulating READS+WRITES(everywhere) pair, `+=` only, never assign after the zero point.
- Binding anti-patterns: never use AMReX's stateful RNG (`amrex/Src/Base/AMReX_Random.H`, [MCNX-RNG-05]); never adopt CarpetX's AoS/host-loop interpolator (`CarpetX/CarpetX/src/interp.hxx:38`, [MCNX-GPU-07]/[MCNX-CTX-07]). Grid access via the `ghext` relative-include idiom proven in `mcnux_cadence.cxx:28` (`#include "../../../CarpetX/CarpetX/src/driver.hxx"`); `REQUIRES CarpetX` alone suffices — never add `REQUIRES AMReX`.
- Grid tallies: unordered atomic accumulation, no bitwise cross-rank/device assertions ([MCNX-RNG-08]) — review-check on every deposition task.
- The Cactus regression harness (flesh) resolves only inside the agent sandbox (`agent_scripts/sandbox/setup.sh`); harness-dependent tests run there, not in the analysis environment. Non-harness gates follow the `agent_scripts/gate_paramcheck.sh` pattern (transient output under `$CACTUSX/GATE/mcnux/`, never in the repo) and get CI wiring per the existing `ci.yml:163-164` precedent.
- Consistency sweep (`specs/conventions-and-units.md:132`) forbids duplicated constant literals — standing review gate; currently clean (`mcnux_units.hxx:77-90,211-222` is the sole physical-constant literal site; everything else is synthetic fixture data).
- WeakLibInterp does zero range-checking by design (`WeakLibInterp/src/eos/wli_eos.H:50-52` clamps only the bracket index, leaves delta unclamped → silent linear extrapolation) — range enforcement is entirely MCNuX-side (T11).

## Tier 1 — Core enablers (all unblocked)

- [ ] T12: Analytic/gray opacity mode + source-agnostic dispatch — per-species `kappa_a0`/`kappa_s0`/`eta_scale` parameters in `MCNuX/param.ccl` (currently none) + Kirchhoff-form analytic coefficients `κ_a(s,E)`, `κ_s(s,E)`, `η(s,E)`; runtime-parameter-selected coefficient interface (table vs analytic) that T10's table path must later satisfy without changing consumers (T14/T18 consume cell-centered coefficients exclusively through it, [MCNX-INT-05]); formulas pinned at `specs/verification-suite-design.md:202-233` ([MCNX-VER-05]); source-agnostic interface requirement `specs/opacity-eos-evaluation.md:118-126`
  - spec: specs/verification-suite-design.md
  - tests: analytic-mode coefficient values vs closed forms (machine tier) as battery rows appended at 71+, SIZE bump, golden regen; dispatch-selection exactness (flipping the source parameter changes values accordingly; exact); compile-time selftests in the new header aggregated via `stub.cxx`.
  - notes: enabling fixture for all fixed-seed/statistical transport benchmarks (T14–T16, T18) without table dependence; only needs the interface shape, so it may precede T10/T11 under sequencing pressure. OPA requires the interface be source-agnostic (`specs/opacity-eos-evaluation.md:119-124`). Promoted to the head of the plan 2026-08-21: it is the sole unblock for T14/T18 and every fixed-seed/stats benchmark. Any constant needed goes to `mcnux_units.hxx` once.
- [ ] T15: Geodesic push: gather + RHS + integrator + `minkowski-freestream` — trilinear-minimum interpolation of vertex-centered ADMBaseX `α, β^i, γ_ij` + order-≥2 derivatives as trivially-copyable device functor (header-only struct passed by value into `ParallelFor`, matching the `mcnux_particles.hxx:105-115` capture pattern); RHS `dx^i/dt`, `dp_i/dt` reusing `MCNuX::spatial_metric_inverse`/`p_t_closure` (`mcnux_tetrad.hxx:93-122`) at every RHS from the same gathered snapshot; integrator free but ≥2nd order; `Δt_push` wiring from `MCNuX_TransportStep` (`MCNuX/schedule.ccl:57`) — home (`mcnux_cadence.cxx` or new `.cxx`) is implementer's choice; first real `Array4`/`MultiFab` access ([MCNX-CTX-01] exemplar) via the `ghext` idiom ([MCNX-GEO-01/03/04])
  - spec: specs/geodesic-propagation.md
  - tests: linear-field gather exactness on synthetic `Array4`s (machine tier, battery rows); flat-spacetime free-stream exactness (machine tier); `minkowski-freestream` harness benchmark (parfile + golden 1e-12; `ActiveThorns = "CarpetX IOUtil MCNuX"`, needs no TestMCNuX) with convergence-order ≥2 leg across Δt, Δt/2, Δt/4.
  - notes: centering confirmed `CarpetX/ADMBaseX/interface.ccl:12,16,20`; gather exemplar `warpx/Source/Particles/Gather/FieldGather.H:348`; uses T13 null closure at every RHS; do NOT template on CarpetX's interpolator ([MCNX-GPU-07]); spec anchor `:73-115`. Same gathered snapshot for every RHS term. Needs T8 + T13; first cross-thorn READS resolves the INHERITS-vs-bare-READS open question empirically. Added 2026-08-21: `CarpetX/CarpetX/src/driver.cxx:430` confirms default vertex centering; `warpx/.../FieldGather.H:2146-2213` is a device-kernel pattern reference only (PIC shape functions, not drop-in trilinear); needs packets in the container to push — see the runtime-container-ownership standing fact (test-gated synthetic seeder if this lands before T14). Unblocks T16, T18a (cell-exit-time tracking), T22b, T23.
- [ ] T6: TestMCNuX Schwarzschild initial data — `SHARES: ADMBaseX` + `EXTENDS KEYWORD initial_data`/`initial_lapse` adding `"Schwarzschild"`; writers `IN ADMBaseX_InitialData`/`ADMBaseX_InitialGauge` with full `WRITES: (everywhere)`; isotropic `ψ=1+M/(2r)`, `γ_ij=ψ⁴δ_ij`, `K_ij=0`, `α=(1−M/(2r))/(1+M/(2r))`, `β^i=0` ([MCNX-VER-02/03])
  - spec: specs/verification-suite-design.md
  - tests: smoke parfile under `MCNuX/test/` (never `TestMCNuX/test/`, [MCNX-BLD-05]) + golden TSV of grid output vs closed forms (golden 1e-12; machine-tier agreement expected). This task empirically confirms cross-repository `EXTENDS KEYWORD` works in this flesh; if it regressed, that is a spec change (fallback own-keyword design), stop and report.
  - notes: fully unblocked — depends only on stable base thorns; anchors `specs/verification-suite-design.md:139-181`, `CarpetX/ADMBaseX/schedule.ccl:12,16,68-104`, `CarpetX/ADMBaseX/interface.ccl:12-20`; shift needs no writer (default `"zero"`). First `.cxx` under `TestMCNuX/src/` — update its `SRCS`. Risk (spec `:456-461`): cross-repository `EXTENDS KEYWORD` mechanism unregressed in flesh — this task confirms it empirically. Added 2026-08-21: both writer groups are `IN ODESolvers_Initial`; shift default at `CarpetX/ADMBaseX/param.ccl:18-22`; targets `metric`/`curv`/`lapse` (`ADMBaseX/interface.ccl:12,14,16`); `TestMCNuX` shell confirmed empty (`param.ccl:1`, `schedule.ccl:1`, `interface.ccl:3`, `make.code.defn:3`). First `TestMCNuX`-activating parfile — T25c's sweep must keep passing. Prerequisite for T16 only.
- [ ] T7: TestMCNuX hydro profiles — `SHARES: HydroBaseX` + `EXTENDS KEYWORD initial_hydro` adding `"uniform sphere"`, `"equilibration box"`; writers `IN HydroBaseX_InitialData` for `(ρ,T,Yₑ)` profiles, `vel=0`, other fields zeroed (`eps`/`press`/`entropy`/`Bvec`/`Avec*`), temperature in MeV; parameters for `M`, sphere center/radius, `(ρ₀,T₀,Yₑ₀)`, ambient values ([MCNX-VER-04])
  - spec: specs/verification-suite-design.md
  - tests: smoke parfile + golden comparing profile fields to formulas (golden tier); grid pinned so sharp-surface cells are ≤1% volume effect (spec `:456-493` assumption 4).
  - notes: independent build+test cycle from T6 (separate keyword family/writer); anchors `specs/verification-suite-design.md:182-201`, `CarpetX/HydroBaseX/interface.ccl:11-29`, `CarpetX/HydroBaseX/schedule.ccl:17,59-75`. Needed by `stats-*`, `interactions-fixedseed`, T29. Added 2026-08-21: keyword at `CarpetX/HydroBaseX/param.ccl:5`, hook group `HydroBaseX/schedule.ccl:17,31,60`; one or two `.cxx` files is implementation freedom (spec `:439-444`).
- [ ] T22a: TRP diffusion kinematics pure functions (standalone math half) — truncated-Gaussian displacement branch selector + antiderivative `F` inversion (≤1e-12 in r̃), causal cap `r_d ≤ Δt′_rem` ([MCNX-TRP-08]); correlated momentum θ₂ draw with 21-point `B_i` table + linear interpolation, `((B−1)/(B+1))^r` form, atom-branch `cosθ₂=1` at `f_free=1` ([MCNX-TRP-10]); uniforms in / kinematics out — no RNG calls inside (T22b owns the 5-draw map); extend `mcnux_trp.hxx` + `append_trp_rows`
  - spec: specs/trapped-regime-treatment.md
  - tests: battery rows at 71+ — F/F⁻¹ round-trip ≤1e-12; branch-selector fixtures; `B_i` interpolation fixtures at table nodes and midpoints; atom-branch pin (exact); compile-time static_asserts in-header.
  - notes: spec anchors `:219-321`. Split this plan run: pure scalar math is unblocked and testable now via battery rows; the integration leg (two-leg position update, [MCNX-TRP-09]) moved to T22b with the rest of the blocked dispatch work. Added 2026-08-21: implementation freedom `:421-439`; reuse `detail::approx_eq`/`rtol_machine` from `mcnux_units.hxx:31-48`; no Eq. 50 bound (spec-deferred `:450-455`).
- [ ] T21b: α selection + TRP parameter surface — per-cell `α = min(1, ξ/(κ_a Δt_c))` pure function, fixed-α override parameter, `κ_a′Δt_c ≤ ξ` per-cell diagnostic predicate; `MCNuX/param.ccl` entries for ξ (default 1), τ_diff (default 3), scheme selector, fixed-α override (names free, semantics binding, spec `:434-436` — param.ccl currently has zero TRP entries); consumes `MCNuX::relabel()` (`mcnux_trp.hxx:60-64`) ([MCNX-TRP-04])
  - spec: specs/trapped-regime-treatment.md
  - tests: selection-rule battery rows (exact: α=1 when `κ_a Δt_c ≤ ξ`, ratio otherwise; diagnostic true after relabeling for all fixtures; override pins α); param-table presence exercised by `unit-selftest` parfile defaults.
  - notes: bitwise-limit benchmark needs T18; α-selection anchor `:145-171`. Re-scoped 2026-08-21: the `trp-alpha-limit-{explicit,relabeled}` benchmark half moved to T21c (needs the T18 event loop); this half is a pure-function increment testable now (`mcnux_trp.hxx:36-41` documents the selection rule as "later tasks"). T21c consumes; T22b reads τ_diff/scheme; T29-relabeled depends on it.
- [ ] T10: Baseline coefficient assembly + νx mapping — `κ_a(s,E)`, `κ_s(s,E)`, `η(s,E)` for νe/ν̄e via `emab_evaluate`/`iso_evaluate`/`eos_evaluate` from `mcnux_opacity.hxx` with Kirchhoff closure using EOS chemical potentials at (ρ,T,Yₑ); νx: `κ_a=0`, `η=0`, `κ_s` = ½-mean of the two electron-type Iso values using `Species::NuX`; plugs into T12's dispatch as the table source; add `hc = 1.239841984e-10 MeV cm` to `mcnux_units.hxx` exactly once ([MCNX-OPA-04/05])
  - spec: specs/opacity-eos-evaluation.md
  - tests: Kirchhoff-closure identity at pinned synthetic-table points; νx zero/half-mean identities (machine tier); `hc` pinned-vs-derived rows; battery rows at current tail, golden regen.
  - notes: depends on T9; NES/Pair/Brem thermal-emission assembly is explicitly out of scope — do not attempt (spec `:340-349`). If the `hc` constant is needed it goes in `mcnux_units.hxx` once. Added 2026-08-21: spec anchors `:193-220` (OPA-04), `:222-239` (OPA-05), `hc` at `:356-361`; T29 is the arbiter of stimulated-absorption/chemical-potential conventions (`:338-379`); must call through T11's range policy before evaluation once T11 lands.
- [ ] T11: Table-range enforcement — transparency floor (ρ<ρ_min ⇒ all coefficients zero, no table touch), component-wise clamping elsewhere, per-axis clamp diagnostic counters, no-NaN guarantee ([MCNX-OPA-06]); `EosInversionResult{T,Error}` hard-fail-in-test-mode protocol ([MCNX-OPA-07]) may be stubbed since baseline transport receives T in MeV externally
  - spec: specs/opacity-eos-evaluation.md
  - tests: range-policy battery rows — out-of-range probes return clamped/zero values with correct per-axis counter increments, never NaN (exact); floor probe proves no table touch; inversion-protocol fixture.
  - notes: WeakLibInterp extrapolates permissively — does zero range-checking by design (`wli_eos.H:50-52`) — so enforcement is entirely MCNuX's (README boundary policy); spec anchor `:174-272`. Inversion leg stub-eligible: baseline path never inverts (spec `:261-263`). Added 2026-08-21: only the `AxisBounds`/`axis_bounds`/`axis_bounds_temperature_mev` hooks exist (`mcnux_opacity.hxx:417-437`); `EosInversionResultMeV` pass-through already exists (`mcnux_opacity.hxx:145-219`, rows 39–41); parallel to T10.

## Tier 2 — Independent fixtures, gates & spec hygiene (unblocked)

- [ ] T28: Spec-corpus text fixes — (a) amend `specs/conventions-and-units.md:129`, which demands `1e-14` relative agreement against the *pinned 10-digit* factor values — arithmetically impossible (truncated literals deviate from full-precision recomputation by up to 2.1e-10; worst: emissivity 2.06e-10); T1 resolved it per [MCNX-CNV-04] (extra digits allowed) with `rtol_pinned=1e-9` vs pinned literals and `rtol_machine=1e-14` for full-precision identities — the amendment states this two-tier tolerance so the `unit-selftest` criterion is satisfiable (leave `:130` round-trip sentence unchanged; do not conflate with the G-uncertainty note at `:92`); (b) fix validator invocation `bash tools/validate_specs.sh` → `bash specs/tools/validate_specs.sh` at `specs/README.md:58`, `specs/README.md:103`, and `specs/conventions-and-units.md:136`
  - spec: specs/conventions-and-units.md
  - tests: `bash specs/tools/validate_specs.sh` exit 0 after the edit (exact; sandbox only — literal-string checks at `validate_specs.sh:175-183` must still match).
  - notes: discovered 2026-08-14 during T1; low urgency — code already carries the rationale (commented in `MCNuX/src/mcnux_units.hxx`). Scope includes authoring the amendment via spec-author (no new spec file; edit in place); bundle both edits in one iteration. Re-scoped 2026-08-21: (b) grew from one to three occurrences.
- [ ] T27a: Validity-machinery gate — gate script running an existing-machinery parfile with `CarpetX::poison_undefined_values = yes` (clean pass) plus a deliberately under-declared READS/WRITES meta-test variant (test-gated routine, STEERABLE=NEVER) asserting the driver's checksum/poison error fires (`specs/carpetx-thorn-integration.md:204-208`)
  - spec: specs/carpetx-thorn-integration.md
  - tests: the gate is the test — exact pass/fail on both legs; transient output under `$CACTUSX/GATE/mcnux/`.
  - notes: currently satisfied by nothing — no parfile sets `poison_undefined_values`, no meta-test exists; needs no new physics. Precedent `agent_scripts/gate_paramcheck.sh`; planner decision taken: gate-now (spec treats the bullet as independent), CI wiring per precedent. Distinct from T30 (two-contributor split proves `+=` accumulation ordering, not READS/WRITES declaration).
- [ ] T27b: Scheduling-mode multi-box artifact — committed reproducible artifact (harness benchmark parfile forcing ≥2 boxes per rank + golden `transport_step_count` output, or a gate script asserting the invocation count) proving exactly one `MCNuX_TransportObserver` (`schedule.ccl:80-86`, `OPTIONS: global`, `mcnux_cadence.cxx:74-89`) invocation per (patch,level) traversal per iteration (`specs/carpetx-thorn-integration.md:209-211`)
  - spec: specs/carpetx-thorn-integration.md
  - tests: golden `transport_step_count` TSV (1e-12) or gate exact count.
  - notes: verified only manually during T4 (64-box rerun, 10 firings in 10 iterations) — not reproducible via `agent_scripts/test.sh` today. May later merge with T23's `ownership-multibox`, but needs no particle code and can close now.
- [ ] T25b: Configure-time negative-path gate — script building ThornList variants of `agent_scripts/sandbox/mcnux.th:14-31` omitting `CarpetX/CarpetX` (and a second omitting the WeakLibInterp provider) and asserting configure failure ([MCNX-BLD-02] verification)
  - spec: specs/build-and-integration.md
  - tests: script exit criteria (exact) — both negative variants fail to configure, positive list still configures.
  - notes: spec anchor `:186-189`; CI wiring per gate precedent.
- [ ] T25c: Activation-split audit sweep + layout audit — script asserting (a) `TestMCNuX` appears in `ActiveThorns` only within `MCNuX/test/*.par` (covers `agent_scripts/gates/*.par` too, [MCNX-BLD-06]), (b) smoke configure/build with a ThornList omitting `MCNuX/TestMCNuX`, (c) layout invariants (`specs/build-and-integration.md:203-206` — exactly two thorns, no `TestMCNuX/test/`, 13 spec files + validator; reuse `validate_specs.sh` for the corpus count rather than reimplement); optional cross-check `agent_scripts/ci/mcnux-fetch.th` vs `agent_scripts/sandbox/mcnux.th` consistency
  - spec: specs/build-and-integration.md
  - tests: sweep script pass/fail (exact); must keep passing after T6/T7 add `TestMCNuX`-activating parfiles under `MCNuX/test/`.
  - notes: relevant once parfiles exist (after T5/T14). Parfiles now exist (`unit-selftest.par`); property currently holds by inspection. Planner decision taken: layout audit folds into this script rather than staying manual. Re-confirmed 2026-08-21: `TestMCNuX` appears in zero `ActiveThorns` lines — the sweep is vacuous until T6/T7.
- [ ] T30: Two-contributor split meta-test — second test-gated synthetic contributor scheduled `IN MCNuX_AddToSourceTerms` (`schedule.ccl:114-121`) depositing a disjoint/overlapping fixture; harness test proving the grid total equals the sum of both contributions regardless of contributor order and that no contributor assigns after zero (`+=` only) ([MCNX-HYD-03] zero-then-add protocol, `specs/hydro-coupling-source-terms.md:248-254`)
  - spec: specs/hydro-coupling-source-terms.md
  - tests: harness golden (1e-12) — totals equal sum of the two fixture lists; a variant with contributor order swapped yields identical goldens.
  - notes: newly tracked 2026-08-21 (spec-covered, previously absent from the plan); unblocked — synthetic fixture suffices; reuses `mcnux_srcterms.hxx` ledger helpers and the `source-zero-add` parfile template.
- [ ] T31: Restart-recovery cycle test for source-term variables — checkpoint mid-run and recover; assert `rad_force`/`lepton_source` (declared `checkpoint="yes"`, `MCNuX/interface.ccl:58,63`) and `transport_step_count` read back identically and the next step's zero-then-add proceeds unchanged ([MCNX-HYD-01], `specs/hydro-coupling-source-terms.md:253-254`); harness (`IO::checkpoint_*`/`IO::recover`) or gate — implementer's call
  - spec: specs/hydro-coupling-source-terms.md
  - tests: recovered-run goldens bitwise/1e-12 identical to the uninterrupted run at the same iteration.
  - notes: newly tracked 2026-08-21 (spec-covered, previously absent from the plan); unblocked. Also exercises the T4 recovery caveat (if CarpetX doesn't mark recovered scalars valid under mixed-error, schedule the init routine also `AT CCTK_POST_RECOVER_VARIABLES`) — resolve it here.

## Tier 3 — Transport chain (blocked; dependency order)

- [ ] T14: Emission sampling: count law + packet creation + `emission-fixedseed` — runtime `PacketContainer` owner (`GetParGDB()` wiring) if T15 has not already done it; `E_p` param in `param.ccl`; packet state schema onto T8 container ([MCNX-PKT-01]); emission count `N_p = g_s √−g ΔV Δt η_b(s)/E_p` with floor/Bernoulli remainder, g=4 applied exactly once at creation via `MCNuX::degeneracy` ([MCNX-PKT-02]); bin-center energy, weight `N=E_p/ν`, uniform position/time draws ([MCNX-PKT-03]); pinned `(q,e,k)` creation draw map (count draw, then k=0..5 per packet; k=4,5 feed `fluid_frame_direction`/`fluid_momentum_to_coordinate`, `mcnux_tetrad.hxx:239-297`) with `K_cell` bit packing in a new `mcnux_emission.hxx` ([MCNX-PKT-05]); id uniqueness via `idcpu`, bit 63 never set, never reused ([MCNX-GPU-04])
  - spec: specs/packet-representation-and-sampling.md
  - tests: `emission-fixedseed` harness benchmark — exact per-packet event/state sequence at seed `1296518744`, single rank, golden 1e-12; draw-map audit trace (exact); count-law determinism and bin-center exactness rows (exact); id-contract check over a pinned multi-step create/remove run (bit 63 clear, pairwise-distinct, immutable) — harness golden vs gate undecided, implementer chooses; single-rank determinism (two runs bitwise-identical populations, `specs/particle-container-and-gpu.md:258-264`).
  - notes: uses T12 analytic η; direction draw consumes T13; anchors spec `:90-162`, `specs/particle-container-and-gpu.md:180-188`. Builds T26's machinery with it. Re-scoped 2026-08-21: T14 is now the deterministic half only (creation + `emission-fixedseed` + id-contract); the 4σ machinery and `stats-emission` moved to T26 so each is independently buildable. `K_cell` packing anchors `:151-158`, assumptions `:232-236`; fluid-state read (ρ,T,Yₑ,u^μ) from HydroBaseX may hit the INHERITS question before T15. Unblocks T26, T19, T20, T23; first real device kernel launch ([MCNX-GPU-02]).
- [ ] T26: Statistical-acceptance shared machinery + `stats-emission` — one shared header (`mcnux_stats.hxx`) computing estimate/expected/σ/z with the 4σ predicate at `N_p=2^20`, seeds `1296518744` (primary)/`20260730` (secondary); per-check values written as grid scalars/norm output; σ formula supplied by each consuming benchmark's leaf spec; `stats-emission` parfile + golden as first consumer ([MCNX-RNG-07], [MCNX-VER-07], [MCNX-PKT-06])
  - spec: specs/rng-and-statistical-acceptance.md
  - tests: `stats-emission` — count/energy/species estimators within 4σ at seed `1296518744`, z-values archived then golden-frozen at 1e-12 (no seed-shopping on a false 4σ failure); battery rows for the z/σ helper on synthetic inputs (exact).
  - notes: build WITH the first stats benchmark (T14) and reuse — do not duplicate per benchmark; listed separately so consolidation is explicit. Per-check estimate/expected/σ/z written as grid scalars/norm outputs; σ formula comes from each consuming benchmark's leaf spec; API shape free (`specs/rng-and-statistical-acceptance.md:125-130`). Re-scoped 2026-08-21: now owns `stats-emission` outright (T14 keeps only the fixed-seed benchmark); depends on T14. Reused by `stats-beam`/`stats-scatterbox`/`stats-diffusion`/`stats-equilibration`/`trp-overlap-consistency`; contract at `specs/verification-suite-design.md:255-279`.
- [ ] T18a: Interaction draw + episode competition + RNG map — `Δt_{s,a} = −ln(1−u_{s,a}) p^t/(κ_{s,a} ν)`; per-cell episode boundaries (creation/step-start, post-scatter, cell-crossing — normative deviation from papers, golden-determining); shorter-of firing vs cell exit and Δt_rem; zero-opacity `+∞` sentinel, no NaN; tie-break to scattering; pinned `(k=0..3)` draw order, undrawn-remainder discard, `event_counter` (`mcnux_particles.hxx:63-65`) incremented per episode ([MCNX-INT-01/02/06])
  - spec: specs/neutrino-matter-interactions.md
  - tests: draw-map audit trace (exact); battery rows with synthetic `p^t`/cell-exit inputs; NaN-free zero-opacity fixture (exact); `interactions-fixedseed` golden parity 1e-12 (needs T7 uniform sphere; lands fully with T18b/c).
  - notes: spec anchors `:83-126,165-179`; coefficients exclusively via T12/T10 interface, cell-centered ([MCNX-INT-05]) — never re-derived. Preserve spec-normative deviations verbatim (per-cell episode redraw, frozen episode kinematics, tie→scattering, spec `:249-271`). Added 2026-08-21: implicit dependency on T15 — per-cell episode boundaries need cell-exit-time tracking, which only exists once T15's push/cell-crossing machinery lands (spec `:236-247` leaves it as implementation freedom); also needs T12 and T14. The exemplar `event_counter` increment at `mcnux_particles.hxx:113` is not INT-02 logic.
- [ ] T18b: Absorption — full packet removal (no weight decay), full-content transfer via `absorption_delta`/`source_from_delta` into the T17 ledger through a real contributor scheduled `IN MCNuX_AddToSourceTerms`, id retirement (never reused) ([MCNX-INT-03])
  - spec: specs/neutrino-matter-interactions.md
  - tests: `stats-beam` — attenuation e^{−κΔ} within 4σ via T26 (analytic κ_a slab, κ_s=0); discreteness (weights never decay, exact); ledger transfer sums exact per event; `interactions-fixedseed` golden.
  - notes: part of the `interactions-fixedseed` golden benchmark (`specs/verification-suite-design.md:244`, needs T7 hydro data). Added 2026-08-21: partial — ledger arithmetic `absorption_delta` (`mcnux_srcterms.hxx:72`) and sign fixtures (`:132-149`, rows 60–64) are done; the event trigger/removal/id retirement is missing; needs T18a.
- [ ] T18c: Elastic scattering — isotropic fluid-frame redraw at fixed ν via T13 tetrad (k=2,3 draws → `fluid_frame_direction`/`fluid_momentum_to_coordinate`, `mcnux_tetrad.hxx:218,239,258`), momentum write-back, `scattering_delta` into ledger ([MCNX-INT-04])
  - spec: specs/neutrino-matter-interactions.md
  - tests: `stats-scatterbox` — isotropy within 4σ via T26, elasticity ν preserved (machine 1e-14); fixed-seed event-sequence parity.
  - notes: reuse T13's tetrad — never reimplement (spec `:136-154`). Added 2026-08-21: partial — tetrad math + `scattering_delta` done; trigger/momentum write-back missing; needs T18a.
- [ ] T19: Conservation-ledger closure check — per-step, per-channel audit: event-side sums vs grid totals for energy/momentum/lepton number, relative 1e-13/step; test-configuration-only allowed; escape tallies (per-species energy/number at outer boundary, README boundary policy) belong to this ledger surface ([MCNX-HYD-05])
  - spec: specs/hydro-coupling-source-terms.md
  - tests: closure flag golden-passed in `emission-fixedseed`/`interactions-fixedseed` configurations (exact pass flag, 1e-13 tolerance).
  - notes: depends on T17 + T18; spec anchors `:177-188,295-296`. Synthetic-event arithmetic validation can land with/after T17; real closure needs real producers. Always-on vs test-only audit computation is an implementer decision (spec `:295-296`).
- [ ] T20: `cadence-lag` benchmark — golden benchmark demonstrating source terms advance exactly once per coarsest Δt with the one-step lag semantics ([MCNX-CTX-05] verification): step-function fluid perturbation at iteration n first affects source terms at n+1; counter increments once per EVOL not per RK stage — requires an active RK integrator (ODESolvers) in the parfile, unlike `source-zero-add`
  - spec: specs/verification-suite-design.md
  - tests: golden parity 1e-12 (counter + source-term tally checksum TSV).
  - notes: depends on T4 + T14/T17; spec anchor `:238-247`. Counter half already exists (`transport_step_count` in `MCNuX/interface.ccl`). Added 2026-08-21: companion anchor `specs/carpetx-thorn-integration.md:196-203`.
- [ ] T16: `schwarzschild-pt` benchmark — parfile + golden data: packet orbits on T6's Schwarzschild background, `p_t = −α²p^t + β^i p_i` (distinct from `p^t`) drift ≤1e-5 at `h=M/16`, `T=100M` ([MCNX-GEO-05])
  - spec: specs/geodesic-propagation.md
  - tests: benchmark with explicit numeric tolerance + convergence order ≥2 across Δt, Δt/2, Δt/4 + golden 1e-12.
  - notes: depends on T6 + T15; separate build+test cycle from both; anchors spec `:117-132`, `specs/verification-suite-design.md:241-242`. The 1e-5 drift bound is a design bound, re-pinnable at golden-creation time per spec `:456-493`. Added 2026-08-21: re-pin at golden creation and document (`specs/verification-suite-design.md:467-473`).
- [ ] T21c: `trp-alpha-limit-{explicit,relabeled}` bitwise-limit benchmarks — α=1 relabeled run reproduces the explicit run BITWISE at fixed seed, single rank ([MCNX-TRP-05], spec `:173-181`; `specs/verification-suite-design.md:247,328,375`)
  - spec: specs/trapped-regime-treatment.md
  - tests: α=1 relabeled run reproduces explicit run BITWISE at fixed seed, single rank; both golden 1e-12.
  - notes: split out of T21b on 2026-08-21; needs T18 (event loop) + T21b (α selection/params).
- [ ] T22b: TRP regime entry + episode dispatch + position/momentum integration + 5-draw map — `κ_s′Δt′_rem > τ_diff` evaluated after EVERY performed scattering (documented deviation, spec `:182-217,507-522`), one diffusion episode per (packet, step), no absorption channel inside; two-leg position update (advection leg `u^i/u^t` + free-streaming leg via T15's pusher, summing exactly to Δt_rem, [MCNX-TRP-09]); tetrad-transformed momentum outcome via T13; fixed five-draw order ([MCNX-TRP-06/07])
  - spec: specs/trapped-regime-treatment.md
  - tests: `stats-diffusion` (three-clause 4σ via T26); `trp-overlap-consistency`; scheme identities; diffusion-leg draw-map audit in `interactions-fixedseed` (`:361-406`).
  - notes: depends on T18. Also needs T13, T15, T22a. Preserve the spec's four logged deviations verbatim (`:443-557`). Added 2026-08-21: also reads T21b's τ_diff/scheme parameters; anchors `:182-217` (TRP-06), `:249-277` (TRP-09), `:323-339` (TRP-07).
- [ ] T23: GPU redistribution + ownership invariants — local `Redistribute`/`RedistributeLocal(N)` inside `MCNuX_TransportStep` after position-changing ops, gated on bounded motion (N derived from an actual per-step motion bound — coordinate speed × Δt / cell width from T15; no design exists yet); after-step ownership invariant: every packet on the rank/level/grid/tile owning its position ([MCNX-GPU-05/06])
  - spec: specs/particle-container-and-gpu.md
  - tests: `ownership-multibox` harness benchmark (≥2 boxes, ownership after step, golden); bounded-motion audit (derived N recorded vs measured max displacement in every deterministic benchmark); single-rank determinism rides on `emission-fixedseed`.
  - notes: depends on T4 step loop + T15 motion; spec anchor `:190-209`; `specs/verification-suite-design.md:245`. Added 2026-08-21: no `Redistribute`/`OK()` call exists anywhere in `MCNuX/`; verification `:246-254`, per-patch assumption `:303-307`; also needs T14; may absorb T27b.
- [ ] T29: `stats-equilibration-{explicit,relabeled}` benchmarks — equilibration-box configuration driving the full emission/absorption/scattering chain to thermal equilibrium; arbiter of OPA's stimulated-absorption and chemical-potential conventions (`specs/opacity-eos-evaluation.md:338-379`) ([MCNX-VER-07])
  - spec: specs/verification-suite-design.md
  - tests: 4σ-then-golden via T26 at `N_p=2^20`, seed `1296518744`; each variant documents its σ formula from its leaf spec.
  - notes: new this plan run — no prior TODO owner. Needs T7 (equilibration box), T10 (table coefficients), the T14/T18 chain, and T21a/T21b for the relabeled variant. Reuse T26 — no new statistical machinery.

## Tier 4 — Deferrable / environment-gated

- [ ] T24: Optional Tmunu contribution — parameter-gated (default off) routine `IN TmunuBaseX_AddToTmunu` (`CarpetX/TmunuBaseX/schedule.ccl:31-33`), MC stress-energy estimator (arXiv:1708.08452v2 Eq. 16 as restated in spec) with exact 8-point cell-to-vertex average `eT(v) += Σ_{d∈{0,1}³} q(v−d)/8` ([MCNX-HYD-06])
  - spec: specs/hydro-coupling-source-terms.md
  - tests: 8-point-average deposition identity (machine tier) + sign fixtures; gated-off default leaves eT* untouched (exact). Never enable with four-force sources in any committed parfile.
  - notes: deferrable; unresolved double-counting question when enabled with four-force sources on evolving spacetime — spec amendment required before that combined path; do not enable both in any committed parfile (spec `:190-222,330-336`). Never mark this and the four-force coupling simultaneously "done" without addressing the open question. Added 2026-08-21: zero `TmunuBaseX` presence in `MCNuX/src` or `*.ccl`; treat as complete-for-standalone only when done.
- [ ] T25d: GPU-backend build of the pinned ThornList + device RNG purity leg ([MCNX-BLD-03/04], [MCNX-GPU-02], [MCNX-RNG-06] device leg)
  - spec: specs/build-and-integration.md
  - tests: GPU-backend configure+build green; precision static_asserts hold; per-packet-operator device-kernel inspection; device-vs-host bitwise equality of `MCNuX::u` over `detail::purity_batch` (`mcnux_rng.hxx:382-397`, exact). GPU-vs-golden norm tolerances pinned at first GPU run (`specs/verification-suite-design.md:488-493`).
  - notes: currently only a CPU/MPI cfg exists in the sandbox. CI compile-only cuda/rocm matrix already exists (`.github/workflows/ci.yml:173-247`); missing legs are the sandbox GPU harness run and the deferred host-vs-device RNG-purity check (deferral recorded in `mcnux_rng.hxx:32-40`; discovered 2026-08-14 during T2 — the CPU/MPI sandbox cannot exercise this leg, so T2 shipped with all host-side purity legs asserted at compile time). GPU-vs-golden norm tolerances explicitly undecided pending first GPU run (spec-recorded open). Added 2026-08-21: whether CI compile-only already discharges [MCNX-BLD-03]'s letter is unresolved in corpus text — keep the stricter reading (sandbox GPU run load-bearing).

## Decisions & non-blocking spec items

- INHERITS vs bare READS: whether `READS:` on `ADMBaseX::`/`HydroBaseX::` groups requires `INHERITS:` in `interface.ccl` under CarpetX flesh — still open (no `INHERITS:` and zero cross-thorn `READS:` exist anywhere in MCNuX); resolved empirically by the first cross-thorn READS (T15, or T14's fluid-state read if it lands first).
- T15-vs-T14 order: whichever lands first owns runtime `PacketContainer` instantiation; if T15 first it needs a test-gated synthetic packet seeder. T14's id-contract test form (harness golden vs non-harness gate) is the implementer's decision.
- RESOLVED at T4 — PARAMCHECK vs schedule-tree ordering: the flesh silently ignores an unresolvable `AFTER ODESolvers_Solve` (no schedule-resolution error precedes the T3 paramcheck abort; verified both with ODESolvers inactive and with `use_subcycling=yes` where only `ODESolvers_Solve_Subcycling` exists). Caveat: with the AFTER target absent the group's placement in `[CCTK_EVOL]` is arbitrary (observed *before* `ODESolvers_Solve_Subcycling`) — T3's guard is therefore load-bearing for [MCNX-CTX-05], not merely defensive; any future subcycling extension must revisit the ordering clause, not just drop the guard.
- RESOLVED at T8 — header/gate permanent home: `stub.cxx` does not retire; each shared header carries its own compile-time selftests (AMReX precision-gate legs live in `mcnux_particles.hxx`), `stub.cxx` keeps the plain double/CCTK_REAL legs and remains the aggregation point including every shared header.
- RESOLVED — `.gitignore` build products unnecessary: multiple sandbox builds (T3–T5) completed with the repo staying clean (build lives in mounted `$CACTUSX` tree per `agent_scripts/sandbox/README.md:39`).
- Gate CI wiring: CLAUDE.md calls gates "sandbox only" but `gate_paramcheck.sh` already runs in CI (`ci.yml:163-164`) — precedent decided: new gates (T27a, T25b, T25c) get CI wiring too.
- T24 completeness: a standalone gated-off Tmunu routine discharges [MCNX-HYD-06] for standalone use only; the spec's own open question (`specs/hydro-coupling-source-terms.md:330-336`) requires a spec amendment (edit in place, not a new spec file) before any combined four-force+Tmunu path.
- [MCNX-BLD-03] GPU obligation: whether CI compile-only already discharges the spec's letter (leaving T25d as extra rigor) or the sandbox run is load-bearing — unresolved in corpus text; T25d keeps the stricter reading.
- `agent_scripts/ci/mcnux-fetch.th` vs `agent_scripts/sandbox/mcnux.th` sync is convention-only — fold an automated cross-check into T25c (listed there as optional) or leave manual.
- Scripted no-duplicate-literal sweep joining `validate_specs.sh` vs staying a manual review gate — tooling scope, not blocking; revisit if a violation ever slips through review.
- T4 recovery caveat (not exercised): if CarpetX doesn't mark recovered scalars valid under mixed-error, schedule the init routine also `AT CCTK_POST_RECOVER_VARIABLES` — now owned by T31.
- Ralph `.ralph/logs` zero-cost `⚠️ no-op` iterations are 0-turn synthetic-orchestrator records, not task failures — awareness only.
- Explicitly spec-deferred (NOT deliverables): packet-count control beyond `E_p` (`specs/packet-representation-and-sampling.md:245-248`); β-dependent Eq. 50 stability bound (`specs/trapped-regime-treatment.md:450-455`); NES/Pair/Brem νx thermal-emission assembly (`specs/opacity-eos-evaluation.md:340-349`); HYD-06 simultaneous-use double-counting (`specs/hydro-coupling-source-terms.md:330-336`).

## Discovered since last plan
